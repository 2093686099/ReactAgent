---
phase: 08-sse-chat-foundation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - frontend/src/components/layout/app-layout.tsx
  - frontend/src/components/sidebar/sidebar.tsx
  - frontend/src/components/chat/chat-area.tsx
  - frontend/src/components/chat/chat-input.tsx
  - frontend/src/components/chat/streaming-dots.tsx
  - frontend/src/components/chat/message-list.tsx
  - frontend/src/components/chat/message-bubble.tsx
  - frontend/src/components/chat/text-segment.tsx
autonomous: true
requirements:
  - CHAT-01
  - CHAT-03
  - CHAT-04
  - CHAT-06
must_haves:
  truths:
    - "AppLayout 渲染侧边栏（240px）和聊天区域（flex-1）的两栏布局"
    - "ChatInput 支持 Enter 发送和 Shift+Enter 换行"
    - "用户消息右对齐有气泡背景，AI 消息左对齐无气泡"
    - "AI 消息中的 Markdown（代码块、列表、表格、链接）正确渲染"
    - "StreamingDots 显示三点脉冲动画"
  artifacts:
    - path: "frontend/src/components/layout/app-layout.tsx"
      provides: "Two-column layout with sidebar and chat area"
      exports: ["AppLayout"]
    - path: "frontend/src/components/chat/chat-input.tsx"
      provides: "Auto-height textarea with Enter send"
      exports: ["ChatInput"]
    - path: "frontend/src/components/chat/message-bubble.tsx"
      provides: "User/AI message rendering with segments"
      exports: ["MessageBubble"]
    - path: "frontend/src/components/chat/text-segment.tsx"
      provides: "Markdown rendering with syntax highlighting"
      exports: ["TextSegment"]
    - path: "frontend/src/components/chat/message-list.tsx"
      provides: "Scrollable message container"
      exports: ["MessageList"]
    - path: "frontend/src/components/chat/streaming-dots.tsx"
      provides: "Typing indicator animation"
      exports: ["StreamingDots"]
  key_links:
    - from: "frontend/src/components/chat/message-bubble.tsx"
      to: "frontend/src/components/chat/text-segment.tsx"
      via: "segments.map rendering"
      pattern: "seg\\.type.*text.*TextSegment"
    - from: "frontend/src/components/chat/text-segment.tsx"
      to: "react-markdown"
      via: "ReactMarkdown component"
      pattern: "ReactMarkdown|react-markdown"
---

<objective>
构建所有 Phase 08 的 UI 组件：两栏布局、消息列表、消息气泡（用户/AI 区分）、Markdown 渲染、输入框、流式加载指示器。

Purpose: 这些组件是聊天界面的视觉层，依赖 Plan 01 创建的类型和 store，将在 Plan 03 中与数据层连接。
Output: 8 个可独立渲染的 React 组件，覆盖 D-01 到 D-10 的所有布局和样式决策。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sse-chat-foundation/08-CONTEXT.md
@.planning/phases/08-sse-chat-foundation/08-UI-SPEC.md
@.planning/phases/08-sse-chat-foundation/08-01-SUMMARY.md
@DESIGN.md

<interfaces>
<!-- Plan 01 创建的类型和 store 接口 — 组件直接使用 -->

From frontend/src/lib/types.ts:
```typescript
export type TextSegment = { type: 'text'; content: string };
export type ToolSegment = { type: 'tool'; name: string; status: 'calling' | 'done' };
export type Segment = TextSegment | ToolSegment;

export interface Message {
  id: string;
  role: 'user' | 'assistant';
  segments: Segment[];
  timestamp: number;
}

export type ChatStatus = 'idle' | 'sending' | 'streaming' | 'error';
```

From frontend/src/stores/chat-store.ts:
```typescript
export const useChatStore = create<ChatState>(...)
// State: messages, status, currentTaskId, activeSessionId
// Actions: addUserMessage, addAssistantMessage, appendToken, finishMessage, setStatus, setCurrentTaskId, reset
```

shadcn/ui 组件已安装: button, textarea, scroll-area, separator, skeleton, sonner
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppLayout + Sidebar + ChatArea + ChatInput + StreamingDots 组件</name>
  <files>
    frontend/src/components/layout/app-layout.tsx
    frontend/src/components/sidebar/sidebar.tsx
    frontend/src/components/chat/chat-area.tsx
    frontend/src/components/chat/chat-input.tsx
    frontend/src/components/chat/streaming-dots.tsx
  </files>
  <read_first>
    frontend/src/app/globals.css
    frontend/src/app/layout.tsx
    frontend/src/lib/types.ts
    frontend/src/stores/chat-store.ts
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/textarea.tsx
    frontend/src/components/ui/separator.tsx
    .planning/phases/08-sse-chat-foundation/08-UI-SPEC.md
    DESIGN.md
  </read_first>
  <action>
1. **创建 `frontend/src/components/layout/app-layout.tsx`**（per D-01, D-02）：
   - 使用 CSS Grid 布局：`grid-template-columns: 240px 1fr`，`min-height: 100vh`
   - 左列放 `<Sidebar />`，右列放 `{children}`（ChatArea）
   - 中间用 `<Separator orientation="vertical" />` 或 `border-right: 1px solid rgba(255,255,255,0.05)` 分隔
   - 导出 `AppLayout` 组件，接收 `children: React.ReactNode`

2. **创建 `frontend/src/components/sidebar/sidebar.tsx`**（per D-01）：
   - 背景 `#08090a`（var(--color-bg-deepest)），占满高度
   - 顶部放 logo 区域：一个简单的 AI icon（lucide `Bot` 或 `Sparkles`）+ 文字 "AI Agent"，颜色 `#62666d`，大小 16px weight 510
   - 下方放"新建会话"按钮：使用 shadcn `<Button variant="ghost">` 样式，图标 lucide `Plus`，文字 "新建会话"，全宽，文字 `#d0d6e0` 15px weight 510
   - 按钮 `onClick` 暂时调用 `useChatStore.getState().reset()`（Phase 10 替换为真正的会话创建）
   - 侧边栏内 padding：`16px`

3. **创建 `frontend/src/components/chat/chat-area.tsx`**：
   - 背景 `#0f1011`（var(--color-bg-panel)），`display: flex; flex-direction: column; height: 100vh`
   - 包含两个区域：
     - 上方：`<MessageList />` 占 `flex: 1; overflow-y: auto`（从 Plan 02 Task 2 导入，暂用 placeholder div）
     - 下方：`<ChatInput />` 固定在底部
   - 接收 `children` 用于放置 MessageList（或直接导入）

4. **创建 `frontend/src/components/chat/chat-input.tsx`**（per D-08, D-09, D-10, CHAT-01, CHAT-06）：
   - 容器：`max-width: 768px; margin: 0 auto; width: 100%; padding: 16px 24px 24px`
   - 上边界：`border-top: 1px solid rgba(255,255,255,0.05)`
   - 内部用相对定位包裹 textarea 和 send 按钮
   - Textarea（基于 shadcn `<Textarea>` 自定义）：
     - 样式：`background: rgba(255,255,255,0.02)`, `border: 1px solid rgba(255,255,255,0.08)`, `border-radius: 8px`
     - padding: `12px 48px 12px 14px`（右侧留空给 send 按钮）
     - 文字：`#f7f8f8` 15px weight 400，placeholder: "输入消息..." 颜色 `#8a8f98`
     - focus 态：border 变为 `rgba(255,255,255,0.12)`
     - min-height: 44px, max-height: 200px
     - 自动撑高：`onInput` 事件中 `el.style.height = 'auto'; el.style.height = Math.min(el.scrollHeight, 200) + 'px'`
     - `resize: none`
   - IME 组合输入处理（中文/日文输入法）：用 `useRef` 维护 `isComposing` 标记，监听 `onCompositionStart` 设为 true，`onCompositionEnd` 设为 false。
   - 键盘处理（per D-08）：`onKeyDown` 中，如果 `e.key === 'Enter' && !e.shiftKey && !isComposing.current`，`e.preventDefault()` 然后调用 submit。Shift+Enter 保持默认换行行为。`isComposing` 为 true 时不响应 Enter，避免中文输入过程中误发送。
   - Send 按钮：absolute 定位在 textarea 右侧 bottom
     - 使用 shadcn `<Button size="icon">`
     - 图标：lucide `ArrowUp`
     - 启用态（输入非空 且 status 不是 sending/streaming）：`background: #5e6ad2`, 图标白色, hover `#828fff`
     - 禁用态：transparent background, 图标 `#62666d`
     - 发送中态（status === 'sending'）：显示 lucide `Loader2` 图标 + `animate-spin`
   - 组件 Props：`onSend: (text: string) => void`
   - 内部 state：`value: string`
   - 发送逻辑：trim value，如果非空调用 `onSend(value.trim())`，清空 input，重置高度

5. **创建 `frontend/src/components/chat/streaming-dots.tsx`**（per CHAT-06, UI-SPEC）：
   - 三个 6px 圆点，颜色 `#8a8f98`
   - CSS animation `dot-pulse`：
     ```css
     @keyframes dot-pulse {
       0%, 80%, 100% { opacity: 0.3; }
       40% { opacity: 1; }
     }
     ```
   - 三个 dot 分别 animation-delay: 0ms, 160ms, 320ms
   - 动画定义放在 globals.css 中，或用 Tailwind 的 `animate-` 自定义
   - 组件用 `<div className="flex items-center gap-1 py-2">` 包裹三个 dot span
  </action>
  <verify>
    <automated>cd /Users/neuron/文稿/2\ 私人/ReActAgents/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/layout/app-layout.tsx` 包含 `grid-template-columns` 或 `grid-cols` 且包含 `240px`
    - `frontend/src/components/sidebar/sidebar.tsx` 包含 `--color-bg-deepest` 或 `#08090a` 且包含 "新建会话"
    - `frontend/src/components/chat/chat-input.tsx` 包含 `onCompositionStart` 和 `onCompositionEnd`（IME 处理）
    - `frontend/src/components/chat/chat-input.tsx` 包含 `onKeyDown` 和 `Enter` 和 `shiftKey` 和 `isComposing`
    - `frontend/src/components/chat/chat-input.tsx` 包含 `max-width` 和 `768`（或 `max-w-3xl`）
    - `frontend/src/components/chat/chat-input.tsx` 包含 `ArrowUp` 或 `Send`（lucide 图标）
    - `frontend/src/components/chat/chat-input.tsx` 包含 `Loader2`（发送中 spinner）
    - `frontend/src/components/chat/chat-input.tsx` 包含 `placeholder` 和 "输入消息"
    - `frontend/src/components/chat/streaming-dots.tsx` 包含 `dot-pulse` 或 animation delay
    - `npx tsc --noEmit` 无类型错误
  </acceptance_criteria>
  <done>布局组件（AppLayout 两栏、Sidebar 骨架）+ 输入组件（ChatInput 自动撑高 + Enter 发送 + 发送按钮状态）+ StreamingDots 加载动画全部就位</done>
</task>

<task type="auto">
  <name>Task 2: MessageList + MessageBubble + TextSegment 消息渲染组件</name>
  <files>
    frontend/src/components/chat/message-list.tsx
    frontend/src/components/chat/message-bubble.tsx
    frontend/src/components/chat/text-segment.tsx
  </files>
  <read_first>
    frontend/src/lib/types.ts
    frontend/src/stores/chat-store.ts
    frontend/src/components/chat/streaming-dots.tsx
    frontend/src/components/chat/chat-area.tsx
    .planning/phases/08-sse-chat-foundation/08-UI-SPEC.md
    DESIGN.md
  </read_first>
  <action>
1. **创建 `frontend/src/components/chat/text-segment.tsx`**（per CHAT-03, UI-SPEC Markdown Rendering）：
   - 接收 Props：`content: string`, `isStreaming?: boolean`
   - 使用 `react-markdown` + `remark-gfm`（GFM 表格、删除线）+ `rehype-highlight`（代码高亮）+ `rehype-sanitize`（XSS 防护，显式白名单过滤）
   - 当 `isStreaming` 为 true 时，不使用 rehype-highlight（避免不完整代码块的解析错误），只用 remark-gfm
   - 自定义 components 映射（覆盖渲染样式）：
     - `p`: margin-bottom 8px, color `#d0d6e0`, 15px
     - `a`: color `#7170ff`, hover 下划线, `target="_blank"` `rel="noopener noreferrer"`
     - `code` (inline): Berkeley Mono 14px, bg `rgba(255,255,255,0.05)`, border-radius 2px, padding `2px 6px`
     - `pre > code` (block): Berkeley Mono 14px, bg `#191a1b`, border `1px solid rgba(255,255,255,0.08)`, border-radius 6px, padding 12px, overflow-x auto
     - `ul/ol`: 15px, gap 8px, left indent 16px
     - `table`: header bg `rgba(255,255,255,0.05)`, cell border `rgba(255,255,255,0.08)`
     - `h1/h2/h3/h4/h5/h6`: 15px weight 590（AI 回复中标题不放大，只加粗 per UI-SPEC）
   - 用 `React.memo` 包裹组件（已完成的消息内容不变，避免重渲染）

2. **创建 `frontend/src/components/chat/message-bubble.tsx`**（per D-03, D-04, CHAT-04）：
   - 接收 Props：`message: Message`, `isStreaming?: boolean`（当前消息是否正在流式输出）
   - **用户消息**（`role === 'user'`）：
     - 容器：`margin-left: auto`（右对齐），`max-width: 85%`
     - 样式：bg `#191a1b`, border `1px solid rgba(255,255,255,0.08)`, border-radius `12px 12px 4px 12px`（右下角小圆角）
     - padding: `8px 16px`
     - 文字：`#f7f8f8` 15px weight 400
     - 直接渲染 `segments[0].content`（用户消息永远只有一个 text segment，不走 Markdown）
   - **AI 消息**（`role === 'assistant'`）：
     - 容器：`margin-right: auto`（左对齐），`max-width: 100%`
     - 样式：无背景、无边框
     - padding: `4px 0`
     - 遍历 `message.segments`：
       - `type === 'text'`: 渲染 `<TextSegment content={seg.content} isStreaming={isStreaming && isLastTextSegment} />`
       - `type === 'tool'`: Phase 08 不渲染，跳过（Phase 09 实现）
     - 底部 AI logo：仅在 `!isStreaming` 时显示。用 lucide `Sparkles` 图标，16px，颜色 `#62666d`，margin-top 8px
   - 用 `React.memo` 包裹，比较 `message.id` 和 `message.segments.length`（仅在 segment 变化时重渲染）

3. **创建 `frontend/src/components/chat/message-list.tsx`**（per CHAT-04, CHAT-05）：
   - 接收 Props：`messages: Message[]`, `status: ChatStatus`, `scrollRef: RefObject<HTMLDivElement>`, `onScroll: () => void`
   - 容器：`flex: 1; overflow-y: auto`，ref 绑定到 `scrollRef`，onScroll 绑定 `onScroll`
   - 内容区域：`max-width: 768px; margin: 0 auto; padding: 0 24px`
   - **空状态**（messages 长度为 0 且 status 为 idle）：
     - 居中显示 "你好，有什么可以帮你的？"（per UI-SPEC Copywriting）
     - 文字颜色 `#8a8f98`，15px weight 400，垂直居中（flex + items-center + justify-center）
   - **消息列表**：
     - 遍历 messages，每条渲染 `<MessageBubble>`
     - 消息间距：16px vertical gap（`space-y-4` 或 `gap-4` in flex column）
     - 最后一条 assistant 消息且 status 为 streaming 时，传 `isStreaming={true}`
   - **StreamingDots**：
     - 当 status 为 'sending' 时（等待第一个 token），在消息列表底部显示 `<StreamingDots />`
  </action>
  <verify>
    <automated>cd /Users/neuron/文稿/2\ 私人/ReActAgents/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/chat/text-segment.tsx` 包含 `ReactMarkdown` 或 `react-markdown`
    - `frontend/src/components/chat/text-segment.tsx` 包含 `remarkGfm` 或 `remark-gfm`
    - `frontend/src/components/chat/text-segment.tsx` 包含 `rehypeHighlight` 或 `rehype-highlight`
    - `frontend/src/components/chat/text-segment.tsx` 包含 `#7170ff`（链接色）
    - `frontend/src/components/chat/text-segment.tsx` 包含 `React.memo` 或 `memo`
    - `frontend/src/components/chat/message-bubble.tsx` 包含 `margin-left: auto` 或 `ml-auto`（用户消息右对齐）
    - `frontend/src/components/chat/message-bubble.tsx` 包含 `#191a1b`（用户气泡背景色）
    - `frontend/src/components/chat/message-bubble.tsx` 包含 `Sparkles`（AI logo 图标）
    - `frontend/src/components/chat/message-list.tsx` 包含 "你好，有什么可以帮你的"（空状态文案）
    - `frontend/src/components/chat/message-list.tsx` 包含 `768` 或 `max-w-3xl`（max-width）
    - `npx tsc --noEmit` 无类型错误
  </acceptance_criteria>
  <done>消息渲染系统完成：TextSegment 支持完整 Markdown（代码高亮、GFM 表格、链接），MessageBubble 区分用户/AI 样式（右对齐气泡 vs 左对齐无气泡），MessageList 管理空状态和消息间距</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI 生成内容 -> Markdown 渲染 | AI 回复可能包含恶意 HTML/JS，需要防 XSS |
| 用户输入 -> 发送到后端 | 用户文本直接传给后端，前端不做内容过滤 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | T (Tampering) / E (Elevation) | TextSegment XSS | mitigate | react-markdown 默认不渲染 raw HTML（`allowedElements` 白名单机制），不设置 `allowDangerousHtml`。rehype-highlight 只操作 code 块。AI 输出中的 `<script>` 标签会被自动转义为文本。 |
| T-08-05 | I (Information Disclosure) | 链接 target="_blank" | mitigate | 所有 `<a>` 标签添加 `rel="noopener noreferrer"` 防止 window.opener 泄漏 |
</threat_model>

<verification>
1. `npx tsc --noEmit` 无类型错误
2. TextSegment 使用 react-markdown 且不设置 allowDangerousHtml
3. MessageBubble 用户消息右对齐有背景，AI 消息左对齐无背景
4. ChatInput 支持 Enter 发送，Shift+Enter 换行
5. MessageList 空状态显示引导文案
</verification>

<success_criteria>
- 8 个 UI 组件全部创建且通过 TypeScript 编译
- 组件样式严格遵循 UI-SPEC 中的像素级规格（颜色、间距、字重、圆角）
- Markdown 渲染覆盖代码块高亮、GFM 表格、链接、列表
- ChatInput 键盘交互正确（Enter 发送 / Shift+Enter 换行）
</success_criteria>

<output>
After completion, create `.planning/phases/08-sse-chat-foundation/08-02-SUMMARY.md`
</output>
