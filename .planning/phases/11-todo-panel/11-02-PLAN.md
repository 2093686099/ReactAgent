---
phase: 11-todo-panel
plan: 02
type: execute
wave: 1
depends_on: [11-01]
files_modified:
  - backend/app/models/chat.py
  - backend/app/core/history.py
  - backend/tests/test_history.py
  - .planning/phases/11-todo-panel/11-CONTEXT.md
autonomous: true
requirements: [TODO-01]
must_haves:
  truths:
    - "`GET /api/sessions/{id}/messages` 响应体包含 `todos` 字段（在 load_history_for_session 所有返回分支中）"
    - "空 state.todos / 无 todos 键 / 无 checkpoint / checkpointer 为 None 四种场景均返回 `todos: []`（不返回 null）"
    - "非空 state.todos 被序列化为 `[{content, status}, ...]`（形状兜底：丢弃未知字段，缺失字段填默认）"
    - "Pydantic `TodoModel` 强校验 status ∈ {pending, in_progress, completed}，非法状态触发 ValidationError"
    - "CONTEXT.md D-08 引用的 token 从 `--color-border-default` 更正为 `--color-border-standard`（globals.css 实际存在）"
  artifacts:
    - path: backend/app/models/chat.py
      provides: "TodoModel Pydantic schema"
      contains: "class TodoModel"
    - path: backend/app/core/history.py
      provides: "todos 字段透出逻辑"
      contains: "channel_values.*todos"
    - path: backend/tests/test_history.py
      provides: "2 个新测试 + 3 个既有测试的 todos 断言"
      contains: "test_messages_endpoint_returns_empty_todos"
    - path: .planning/phases/11-todo-panel/11-CONTEXT.md
      provides: "D-08 token 更正为 --color-border-standard"
      contains: "--color-border-standard"
  key_links:
    - from: backend/app/core/history.py
      to: "LangGraph AsyncPostgresSaver checkpoint"
      via: "ckpt_tuple.checkpoint['channel_values']['todos'] 同事务读"
      pattern: "channel_values.*todos"
    - from: backend/app/models/chat.py
      to: backend/app/core/history.py
      via: "TodoModel 用于契约声明（Path A：不绑 response_model，history.py 返回 dict）"
      pattern: "class TodoModel"
---

<objective>
后端契约扩展：`GET /api/sessions/{id}/messages` 响应体增加 `todos: list[{content, status}]` 字段，读取 LangGraph checkpoint `channel_values["todos"]` 同事务同源；新增 Pydantic `TodoModel` 声明契约；pytest 集成测试覆盖空/有 todos / 无 checkpointer 四条分支；顺带修正 CONTEXT.md D-08 引用的 DESIGN token（`--color-border-default` 不存在，改为 `--color-border-standard`）。

Purpose: TODO-01 需要前端能从历史端点还原 todos（D-10/D-11/D-12）；此 plan 是前端串接的前置条件。D-08 token 修正是 11-04 前端实现不踩坑的前置（globals.css 只有 subtle/standard/focus 三态）。
Output: 后端历史端点返回结构扩展 + Pydantic 模型 + 4 条测试断言，CONTEXT.md D-08 与 globals.css 对齐。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-todo-panel/11-CONTEXT.md
@.planning/phases/11-todo-panel/11-RESEARCH.md
@.planning/phases/11-todo-panel/11-PATTERNS.md
@.planning/phases/11-todo-panel/11-01-SUMMARY.md
@backend/app/models/chat.py
@backend/app/core/history.py
@backend/tests/test_history.py
@backend/tests/fixtures/checkpoint_factory.py

<interfaces>
<!-- langchain 上游契约（不改，PATTERNS.md 已验证） -->
```python
# .venv/lib/python3.12/site-packages/langchain/agents/middleware/todo.py:25-42
class Todo(TypedDict):
    content: str
    status: Literal["pending", "in_progress", "completed"]
```

<!-- 现有 messages 读取模式（history.py:145-155）—— todos 照抄 -->
```python
raw = (ckpt_tuple.checkpoint or {}).get("channel_values", {}).get("messages", []) or []
```

<!-- 现有 history.py 四个返回分支（PATTERNS.md 标注） -->
<!-- Branch 1: history.py:128-134（session_svc.get_session 异常/None）-->
<!-- Branch 2: history.py:138-143（checkpointer is None）-->
<!-- Branch 3: history.py:151-155（正常 + truncate 分支）-->
<!-- Branch 4: 无 ckpt_tuple 返回时（aget_tuple 返回 None）-->
<!-- 以上 4 处均需 "todos": [] -->

<!-- D-11 路径 A 选定（最小改动） -->
<!-- 在 history.py 内用 TodoModel 做形状校验（可选），或直接返回 dict；端点不绑 response_model。-->
<!-- PATTERNS.md §backend/app/models/chat.py §落地 D-11 的两条路径 —— 明确选 A -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 Pydantic TodoModel + 修正 CONTEXT.md D-08 token</name>
  <files>
    backend/app/models/chat.py
    .planning/phases/11-todo-panel/11-CONTEXT.md
  </files>
  <read_first>
    - backend/app/models/chat.py（完整文件，确认现有 BaseModel 风格 + 已有 import）
    - frontend/src/app/globals.css:25-27（确认 token 清单只有 subtle/standard/focus）
    - .planning/phases/11-todo-panel/11-CONTEXT.md（定位 D-08 原文 "border-default" 行）
  </read_first>
  <behavior>
    - `from app.models.chat import TodoModel` 可成功 import
    - `TodoModel(content="a", status="pending")` 通过校验
    - `TodoModel(content="a", status="bogus")` 抛 Pydantic ValidationError
    - `TodoModel(content="a", status="in_progress")` / `completed` 通过
    - CONTEXT.md D-08 pending 分支不再出现 `--color-border-default`，改为 `--color-border-standard`
    - CONTEXT.md 其他部分（D-01~D-15 除 D-08 外 + Claude's Discretion + Deferred）保持原文
  </behavior>
  <action>
    **子任务 A —— 新增 TodoModel（per D-11 选定路径 A）：**

    在 `backend/app/models/chat.py` 末尾追加：

    ```python
    from typing import Literal
    # 如果文件已有 `from pydantic import BaseModel`，不重复 import

    class TodoModel(BaseModel):
        """langchain TodoListMiddleware Todo TypedDict 的 Pydantic 镜像。

        用于声明 GET /api/sessions/{id}/messages 响应 todos 字段契约。
        """
        content: str
        status: Literal["pending", "in_progress", "completed"]
    ```

    **路径 A 理由**（显式记录，per PATTERNS.md §backend/app/models/chat.py 表）：
    - 现状：端点 `GET /sessions/{id}/messages` 未绑定 `response_model=`（PATTERNS.md §backend/app/api/sessions.py 验证）
    - 路径 A 只加 `TodoModel` 声明契约，改动面最小，不牵涉 Phase 10 既有 messages/active_task 的建模
    - 路径 B（加 `HistoryResponseModel` 并绑 response_model）会扩大改动到 Phase 10 范围，违反 CLAUDE.md §3 外科手术原则
    - 两条路径都满足 D-11 "响应体变更需同步更新 Pydantic response model"

    **子任务 B —— 修正 CONTEXT.md D-08 token：**

    用 Edit 工具，在 `.planning/phases/11-todo-panel/11-CONTEXT.md` 内把 pending 圆圈那行的 `var(--color-border-default)` 原地替换为 `var(--color-border-standard)`。

    原行（D-08 pending 分支，CONTEXT.md 第 37 行附近）：
    ```
      - `pending`：空心圆，`border: 1.5px solid var(--color-border-default)`
    ```
    改为：
    ```
      - `pending`：空心圆，`border: 1.5px solid var(--color-border-standard)`
    ```

    **理由**（记录在 edit 之上保留的文本不变）：globals.css:25-27 只定义 subtle/standard/focus 三个 border token，D-15 禁止新增 token；选 `--color-border-standard` (0.08) 是 RESEARCH.md BLOCKER-1 推荐值（对比度适中，用户已在讨论中 lock）。

    **外科手术原则：**
    - 只改这一行文字，D-08 的 in_progress / completed 分支与前后段落保持原样
    - 不动 RESEARCH.md / PATTERNS.md（它们已用 `--color-border-standard`，无须动）
  </action>
  <verify>
    <automated>cd backend && python -c "from app.models.chat import TodoModel; TodoModel(content='a', status='pending'); TodoModel(content='b', status='in_progress'); TodoModel(content='c', status='completed'); import pytest; ex = None; exec('try:\n    TodoModel(content=\"x\", status=\"bogus\")\nexcept Exception as e:\n    ex = e'); assert ex is not None, 'invalid status must raise'"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `grep -n "class TodoModel" backend/app/models/chat.py` 返回 1 条
    - [ ] `grep -n "Literal\[\"pending\", \"in_progress\", \"completed\"\]" backend/app/models/chat.py` 返回 1 条
    - [ ] `grep -c "color-border-default" .planning/phases/11-todo-panel/11-CONTEXT.md` 返回 0（完全消除）
    - [ ] `grep -n "color-border-standard" .planning/phases/11-todo-panel/11-CONTEXT.md` 至少返回 1 条（替换到位）
    - [ ] `cd backend && python -c "from app.models.chat import TodoModel"` 成功 import
  </acceptance_criteria>
  <done>TodoModel 可用；CONTEXT.md D-08 的 token 引用合法（globals.css 实际存在）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: history.py 读 channel_values['todos'] + 四分支返回 todos</name>
  <files>backend/app/core/history.py</files>
  <read_first>
    - backend/app/core/history.py（完整文件；重点 line 128-155 四处 return）
    - backend/tests/test_history.py:165, 178, 200（确认既有测试结构，供 Task 3 添加断言）
  </read_first>
  <behavior>
    - 正常路径（有 checkpoint + 有 todos）：返回 dict 含 `"todos": [...]`，形状 `[{content, status}]`
    - 空 todos（checkpoint 有 messages 但 state.todos 为 `[]` 或缺失键）：返回 `"todos": []`
    - `checkpointer is None` 分支：返回 `"todos": []`
    - `session_svc.get_session` 返回 None 分支：返回 `"todos": []`
    - 未知字段兜底：raw_todos 元素含 `{"content":"x","status":"pending","extra":"ignored"}` → 仅透出 `content` + `status`，丢弃 `extra`
    - 缺失字段兜底：raw_todos 元素为 `{"content":"x"}`（无 status）→ 返回 `{"content":"x","status":"pending"}`（默认值，避免 500）
    - 非 dict 元素（None / str / int）→ 跳过（filter out，不 raise）
  </behavior>
  <action>
    在 `backend/app/core/history.py` 的 `load_history_for_session` 内做外科手术：

    **Step 1 — 在读 messages 附近新增 todos 读取 + 形状兜底**（参考 PATTERNS.md §backend/app/core/history.py 完整模板）：

    在既有 `raw = (ckpt_tuple.checkpoint or {}).get("channel_values", {}).get("messages", []) or []` 之后紧跟：

    ```python
    raw_todos = (ckpt_tuple.checkpoint or {}).get("channel_values", {}).get("todos", []) or []

    # Todo 形状来自 langchain TodoListMiddleware：{"content": str, "status": str}
    # 兜底（RESEARCH.md Pitfall 3 + forward-compat）：
    #   - 丢弃非 dict 元素
    #   - 只透出 content / status，丢弃未知字段
    #   - 字段缺失给默认值，避免 500
    todos: list[dict] = [
        {
            "content": t.get("content", ""),
            "status": t.get("status", "pending"),
        }
        for t in raw_todos
        if isinstance(t, dict)
    ]
    ```

    **Step 2 — 四个 return 分支都加 `"todos": [...]`**

    用 Grep 定位 `history.py` 中所有 `return {` 语句；对于每个 return dict：

    - 正常+truncate 路径（既有 line ~151-155）：加 `"todos": todos`
    - `checkpointer is None` 分支（line ~138-143）：加 `"todos": []`
    - `session_svc.get_session` 未返回 session 分支（line ~128-134）：加 `"todos": []`
    - 任何 `ckpt_tuple is None` / 没有 checkpoint 的兜底分支：加 `"todos": []`

    按字段**插入位置统一在 `"messages"` 键之后**，与 PATTERNS.md 模板一致：

    ```python
    return {
        "messages": messages_to_segments(raw),
        "todos": todos,           # ← 新增
        "active_task": active_task,
        "truncate_after_active_task": truncate,
    }
    ```

    空分支示例：

    ```python
    return {
        "messages": [],
        "todos": [],              # ← 新增
        "active_task": None,
        "truncate_after_active_task": False,
    }
    ```

    **外科手术原则：**
    - 不动既有 messages / active_task / truncate_after_active_task 的逻辑
    - 不重构（CLAUDE.md §3），只追加字段
    - 不引入 `TodoModel` 做运行时校验（路径 A：history.py 返回 dict，前端负责形状消费）
    - 不动 `session_svc` / `task_bus` 相关逻辑
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_history.py -x</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `grep -c "\"todos\"" backend/app/core/history.py` ≥ 4（四个 return 分支各 1 处 + raw_todos 变量行/todos 构造）
    - [ ] `grep -n "raw_todos" backend/app/core/history.py` 返回至少 1 行（新增变量）
    - [ ] `grep -n "channel_values.*todos" backend/app/core/history.py` 返回 1 行
    - [ ] `grep -n "isinstance(t, dict)" backend/app/core/history.py` 返回 1 行（兜底 filter）
    - [ ] 既有测试仍然通过：`cd backend && pytest tests/test_history.py -x` 无 fail
    - [ ] `active_task` / `truncate_after_active_task` 字段在所有 return 处仍在（未被误删）：`grep -c "active_task" backend/app/core/history.py` 不低于改动前数值
  </acceptance_criteria>
  <done>history.py 所有 return 分支都透出 `todos` 字段；空/非空场景覆盖；既有测试通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 新增 2 条 todos 集成测试 + 3 处既有测试追加 todos 断言</name>
  <files>backend/tests/test_history.py</files>
  <read_first>
    - backend/tests/test_history.py（完整文件，定位 `test_truncate_when_active_task` / `test_no_truncate_when_no_active_task` / `test_load_history_when_checkpointer_is_none` / `test_load_history_when_no_checkpoint` 的行号和结构）
    - backend/tests/fixtures/checkpoint_factory.py（11-01 已扩展完，确认 `todos` 参数就绪）
    - backend/app/core/history.py（Task 2 改完，确认四个 return 分支都有 todos）
  </read_first>
  <behavior>
    - 新 test 1：`test_messages_endpoint_returns_empty_todos` —— 不传 todos 参数给 fixture → result["todos"] == []
    - 新 test 2：`test_messages_endpoint_returns_todos_from_checkpoint` —— 传入 3 条 fake_todos → result["todos"] 长度为 3，首条内容 `{"content":"step A","status":"completed"}`，次条 status == "in_progress"
    - 既有 test：`test_no_truncate_when_no_active_task`（line 165 附近）追加 `assert result["todos"] == []`
    - 既有 test：`test_load_history_when_checkpointer_is_none`（line 178 附近）追加 `assert result["todos"] == []`
    - 既有 test：`test_load_history_when_no_checkpoint`（line 200 附近）追加 `assert result["todos"] == []`
    - 全套测试绿（包括原 active_task / truncate / messages 断言不被破坏）
  </behavior>
  <action>
    **子任务 A —— 新增两个测试函数**（照抄 PATTERNS.md §backend/tests/test_history.py 完整模板，放在既有 `test_truncate_when_active_task` 函数之后）：

    ```python
    @pytest.mark.asyncio
    async def test_messages_endpoint_returns_empty_todos(monkeypatch):
        """todos 字段始终存在；state 无 todos 键时返回 []（RESEARCH.md Pitfall 3）"""
        from app.core import history as history_mod

        raw = [make_human("hi"), make_ai(content="done")]
        fake_ckptr = AsyncMock()
        fake_ckptr.aget_tuple = AsyncMock(return_value=make_checkpoint_tuple(raw))  # 不传 todos
        monkeypatch.setattr(history_mod.db, "checkpointer", fake_ckptr)

        session_svc = AsyncMock()
        session_svc.get_session = AsyncMock(return_value={
            "session_id": "s1", "user_id": "u1", "title": "", "last_task_id": None,
        })

        result = await history_mod.load_history_for_session("u1", "s1", session_svc)
        assert result["todos"] == []


    @pytest.mark.asyncio
    async def test_messages_endpoint_returns_todos_from_checkpoint(monkeypatch):
        """state.todos 有内容时正确序列化为 {content, status} dict"""
        from app.core import history as history_mod

        raw = [make_human("plan 3 steps")]
        fake_todos = [
            {"content": "step A", "status": "completed"},
            {"content": "step B", "status": "in_progress"},
            {"content": "step C", "status": "pending"},
        ]
        fake_ckptr = AsyncMock()
        fake_ckptr.aget_tuple = AsyncMock(
            return_value=make_checkpoint_tuple(raw, todos=fake_todos)
        )
        monkeypatch.setattr(history_mod.db, "checkpointer", fake_ckptr)

        session_svc = AsyncMock()
        session_svc.get_session = AsyncMock(return_value={
            "session_id": "s1", "user_id": "u1", "title": "", "last_task_id": None,
        })

        result = await history_mod.load_history_for_session("u1", "s1", session_svc)
        assert len(result["todos"]) == 3
        assert result["todos"][0] == {"content": "step A", "status": "completed"}
        assert result["todos"][1]["status"] == "in_progress"
        assert result["todos"][2]["status"] == "pending"
    ```

    **子任务 B —— 既有三个测试追加 todos 断言**（用 Edit 做精准追加，不动原断言）：

    - 在 `test_no_truncate_when_no_active_task` 末尾（最后一个 assert 后）加 `assert result["todos"] == []`
    - 在 `test_load_history_when_checkpointer_is_none` 末尾加 `assert result["todos"] == []`
    - 在 `test_load_history_when_no_checkpoint` 末尾加 `assert result["todos"] == []`

    **外科手术原则：**
    - 不改既有 monkeypatch / AsyncMock 结构
    - 不改 `make_checkpoint_tuple` 的已有调用（11-01 已做向后兼容）
    - 不 import 新库（AsyncMock / make_checkpoint_tuple / make_human / make_ai 在文件顶部已 import）
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_history.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `grep -n "def test_messages_endpoint_returns_empty_todos" backend/tests/test_history.py` 返回 1 行
    - [ ] `grep -n "def test_messages_endpoint_returns_todos_from_checkpoint" backend/tests/test_history.py` 返回 1 行
    - [ ] `grep -c "result\[\"todos\"\] == \[\]" backend/tests/test_history.py` ≥ 3（三处既有测试 + 新 empty 测试）
    - [ ] `grep -n "step A" backend/tests/test_history.py` 返回 1 行（新非空测试的 fake data）
    - [ ] `cd backend && pytest tests/test_history.py -x -v` 全绿，且 pytest 行数显示至少 +2 新测试
    - [ ] `cd backend && pytest` 全套绿（冒烟，确保没 break 其他模块）
  </acceptance_criteria>
  <done>5 条测试（2 新 + 3 补 assertion）全绿；TODO-01 的后端侧契约验证闭环。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (browser) → API | HTTP GET /api/sessions/{session_id}/messages；已在 Phase 10 Plan 01 加 user_id 隔离（session_svc.session_exists）|
| API → PostgreSQL checkpointer | 读 AsyncPostgresSaver.aget_tuple，数据源可信（项目自有 DB） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | I (Information disclosure / IDOR) | GET /api/sessions/{id}/messages 响应 todos 字段 | accept | Phase 10 Plan 01 已覆盖：`session_svc.session_exists(session_id, user_id=user_id)` 在 sessions.py:98 强制隔离，跨用户访问返回 404；本 plan 仅对响应体加字段（todos），不改授权路径，也不放宽端点。todos 内容来自同一 session 的 LangGraph state，不含 PII，风险边际为零 |
| T-11-02 | T (Tampering via forged checkpoint shape) | raw_todos 元素可能来自 langchain 版本升级后的新字段 | mitigate | history.py 形状兜底：`isinstance(t, dict)` filter + 只透出 `content`/`status` 字段（PATTERNS.md §history.py 兜底代码）；非预期字段被丢弃，不影响响应 schema |
| T-11-03 | D (Denial via malformed todos) | checkpoint `channel_values["todos"]` 为 None / 字符串 / 其他类型 | mitigate | `.get("todos", []) or []` 双层兜底（key 缺失 → []，值为 None → []）；非 dict 元素被 filter 掉（RESEARCH.md Pitfall 3）；返回 `todos: []` 而非 500 |
</threat_model>

<verification>
```bash
# 契约验证
cd backend && pytest tests/test_history.py -x -v

# 全套冒烟
cd backend && pytest

# 形状验证
cd backend && python -c "from app.models.chat import TodoModel; TodoModel(content='a', status='pending')"

# token 修正验证
grep -c "color-border-default" .planning/phases/11-todo-panel/11-CONTEXT.md   # 应 = 0
```
</verification>

<success_criteria>
- `GET /api/sessions/{id}/messages` 响应体 schema 扩展：新增 `todos: list[{content, status}]`（空/非空两种形态均测试通过）
- `TodoModel` 可导入，status 三态枚举校验
- history.py 四个 return 分支都透出 todos 字段（无遗漏）
- CONTEXT.md D-08 引用的 DESIGN token 与 globals.css 对齐
- 后端全套测试绿
</success_criteria>

<output>
After completion, create `.planning/phases/11-todo-panel/11-02-SUMMARY.md` with:
- 新契约形状示例（空 + 非空两条）
- TodoModel 定义位置
- history.py 四个 return 分支修改列表
- 新增测试名称 + 既有测试追加断言位置
- D-08 token 修正前后对照
</output>
