---
phase: 11-todo-panel
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/fixtures/checkpoint_factory.py
autonomous: true
requirements: [TODO-01]
must_haves:
  truths:
    - "旧调用 `make_checkpoint_tuple(messages)` 保持签名兼容（`todos=None` 默认），test_history.py 既有 3 处调用不需修改"
    - "`make_checkpoint_tuple(messages, todos=[...])` 在返回的 `checkpoint.channel_values` 中写入 `todos` 键"
    - "`todos=None` 时 `channel_values` 不含 `todos` 键（模拟从未 write_todos 的 checkpoint，对应 RESEARCH.md Pitfall 3）"
    - "`todos=[]` 时 `channel_values` 含 `todos: []`"
  artifacts:
    - path: backend/tests/fixtures/checkpoint_factory.py
      provides: "Checkpoint fixture with optional todos parameter"
      contains: "todos: list"
  key_links:
    - from: backend/tests/fixtures/checkpoint_factory.py
      to: backend/tests/test_history.py
      via: "make_checkpoint_tuple(raw, todos=...) 下一个 plan 的新测试调用"
      pattern: "make_checkpoint_tuple\\(.*todos"
---

<objective>
扩展 `make_checkpoint_tuple` fixture 使其接受可选 `todos` 参数，为 11-02 的 `test_messages_endpoint_returns_todos_*` 两条集成测试铺路。

Purpose: Wave 0 先行铺设测试基础设施，下游 11-02 才能写出 `make_checkpoint_tuple(raw, todos=fake_todos)` 的断言。此改动向后兼容（todos 默认 None），test_history.py 既有调用不需动。
Output: 扩展后的 fixture 函数。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-todo-panel/11-CONTEXT.md
@.planning/phases/11-todo-panel/11-RESEARCH.md
@.planning/phases/11-todo-panel/11-PATTERNS.md
@backend/tests/fixtures/checkpoint_factory.py

<interfaces>
<!-- 现有 make_checkpoint_tuple 签名（backend/tests/fixtures/checkpoint_factory.py:57-64） -->
```python
def make_checkpoint_tuple(messages: list[Any]):
    """构造一个与 AsyncPostgresSaver.aget_tuple() 返回值形状相同的对象。"""
    return SimpleNamespace(
        checkpoint={"channel_values": {"messages": messages}},
    )
```

<!-- 既有调用点（不改）： -->
<!-- backend/tests/test_history.py:124, 154, 192 均为 `make_checkpoint_tuple(raw)` 单参数调用 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 make_checkpoint_tuple 支持可选 todos 参数</name>
  <files>backend/tests/fixtures/checkpoint_factory.py</files>
  <read_first>
    - backend/tests/fixtures/checkpoint_factory.py（完整文件，确认现有结构）
    - backend/tests/test_history.py（搜索 `make_checkpoint_tuple(` 所有调用点，确认都是单参数，保持向后兼容）
  </read_first>
  <behavior>
    - 旧调用 `make_checkpoint_tuple([msg1, msg2])` 返回 `checkpoint.channel_values == {"messages": [...]}`，无 `todos` 键
    - 新调用 `make_checkpoint_tuple([msg1], todos=None)` 与旧调用等价（channel_values 不含 todos 键）
    - 新调用 `make_checkpoint_tuple([msg1], todos=[])` 返回 `channel_values == {"messages": [...], "todos": []}`
    - 新调用 `make_checkpoint_tuple([msg1], todos=[{"content":"a","status":"pending"}])` 返回 `channel_values["todos"]` 为传入列表
  </behavior>
  <action>
    按 PATTERNS.md §`backend/tests/fixtures/checkpoint_factory.py` 模板扩展签名，实施 D-10/D-11 前置基础设施：

    ```python
    def make_checkpoint_tuple(messages: list[Any], todos: list[dict] | None = None):
        """构造一个与 AsyncPostgresSaver.aget_tuple() 返回值形状相同的对象。

        todos=None → channel_values 不含 "todos" 键（模拟从未触发 write_todos 的 checkpoint）
        todos=[] → channel_values 含 "todos": []
        todos=[...] → channel_values 含 "todos": [...]
        """
        channel_values: dict = {"messages": messages}
        if todos is not None:
            channel_values["todos"] = todos
        return SimpleNamespace(checkpoint={"channel_values": channel_values})
    ```

    **外科手术原则（CLAUDE.md §3）：**
    - 只改 `make_checkpoint_tuple` 函数本身，不动文件其他部分
    - 不新增 import（`list`/`dict` 内置类型够用；若项目原有 `from __future__ import annotations` 或 `Any` 已 import，保持原样不动）
    - docstring 按上方模板写中文（项目既有 docstring 风格一致）
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_history.py -x</automated>
  </verify>
  <acceptance_criteria>
    - [ ] 既有测试全绿：`cd backend && pytest tests/test_history.py -x` 输出 "3 passed"（或既有测试数量）
    - [ ] `grep -n "todos: list" backend/tests/fixtures/checkpoint_factory.py` 至少 1 处命中（签名扩展）
    - [ ] `grep -n "channel_values\[\"todos\"\]" backend/tests/fixtures/checkpoint_factory.py` 至少 1 处命中（条件写入）
    - [ ] `grep -c "make_checkpoint_tuple" backend/tests/fixtures/checkpoint_factory.py` 返回 1（只一处定义，不误增副本）
    - [ ] 现有调用点不需修改：`grep -rn "make_checkpoint_tuple(" backend/tests/ | grep -v checkpoint_factory.py` 所有命中均仍为单参数形式（测试通过即证明）
  </acceptance_criteria>
  <done>fixture 支持 `todos=None|[]|[...]` 三态；既有 test_history 套件通过；11-02 能调用 `make_checkpoint_tuple(raw, todos=fake_todos)`。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → production code | 本 plan 只动测试 fixture，无生产代码路径；无外部信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-00 | (N/A) | test fixture only | accept | 纯测试脚手架，无攻击面；无用户输入、无数据持久化 |
</threat_model>

<verification>
```bash
cd backend && pytest tests/test_history.py -x
grep -n "todos" backend/tests/fixtures/checkpoint_factory.py
```
</verification>

<success_criteria>
- `make_checkpoint_tuple` 签名含 `todos: list[dict] | None = None`
- 既有 test_history.py 所有测试保持通过
- 11-02 的两条新测试能直接使用新 fixture（由 11-02 验证）
</success_criteria>

<output>
After completion, create `.planning/phases/11-todo-panel/11-01-SUMMARY.md` with:
- Fixture 新签名
- 既有测试通过证据
- 下游（11-02）调用方式示例
</output>
