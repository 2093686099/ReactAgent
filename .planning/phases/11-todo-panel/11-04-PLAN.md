---
phase: 11-todo-panel
plan: 04
type: execute
wave: 3
depends_on: [11-03]
files_modified:
  - frontend/src/app/globals.css
  - frontend/src/components/todo/todo-item.tsx
  - frontend/src/components/todo/todo-list.tsx
  - frontend/src/components/todo/todo-drawer.tsx
  - frontend/src/components/todo/todo-toggle-button.tsx
  - frontend/src/components/chat/chat-area.tsx
  - frontend/src/components/layout/app-layout.tsx
autonomous: true
requirements: [TODO-01, TODO-02]
must_haves:
  truths:
    - "Drawer 可见：drawer open 时 AppLayout grid `[240px_1fr_320px]`，drawer close 时 `[240px_1fr]`，transition 200ms ease-out"
    - "AppLayout 为 client component（`\"use client\"`）且顶层 useEffect 调 `useUIStore.persist.rehydrate()`，SSR 首渲染无 hydration mismatch"
    - "TodoItem 三态图标用 DESIGN.md token：pending 边框 `--color-border-standard`、in_progress stroke `--color-accent`、completed 填充 `--color-accent` + 白色 Check"
    - "TodoItem 文案颜色固定 `--color-text-secondary`（不随状态变），状态切换 150ms transition"
    - "TodoList 空态文案 15px + `--color-text-tertiary`，与 message-list idle 空态一致"
    - "Chat header 右上角 TodoToggleButton 调 `useUIStore.toggleDrawer()`，无红点/徽标"
    - "globals.css 新增 `@keyframes todoEnter`（opacity 0→1 + translateY 4px→0，200ms ease-out）供 TodoItem 使用；不新增任何 color/border token"
  artifacts:
    - path: frontend/src/app/globals.css
      provides: "@keyframes todoEnter"
      contains: "@keyframes todoEnter"
    - path: frontend/src/components/todo/todo-item.tsx
      provides: "三态图标 + todo 单条渲染"
      contains: "status === \"in_progress\""
    - path: frontend/src/components/todo/todo-list.tsx
      provides: "列表 + 空态"
      contains: "todos.map"
    - path: frontend/src/components/todo/todo-drawer.tsx
      provides: "抽屉容器 + 标题 + 关闭按钮"
    - path: frontend/src/components/todo/todo-toggle-button.tsx
      provides: "chat 顶栏按钮 + aria-pressed"
      contains: "aria-pressed"
    - path: frontend/src/components/chat/chat-area.tsx
      provides: "加 header 挂 TodoToggleButton"
      contains: "<header"
    - path: frontend/src/components/layout/app-layout.tsx
      provides: "条件式 grid + rehydrate useEffect"
      contains: "use client"
  key_links:
    - from: frontend/src/components/layout/app-layout.tsx
      to: frontend/src/stores/ui-store.ts
      via: "useUIStore((s) => s.todoDrawerOpen) + useEffect(() => useUIStore.persist.rehydrate(), [])"
      pattern: "todoDrawerOpen|persist.rehydrate"
    - from: frontend/src/components/chat/chat-area.tsx
      to: frontend/src/components/todo/todo-toggle-button.tsx
      via: "<header> 右上角挂 toggle 按钮"
      pattern: "TodoToggleButton"
    - from: frontend/src/components/todo/todo-item.tsx
      to: frontend/src/app/globals.css
      via: "--color-accent / --color-border-standard / --color-text-secondary CSS 变量引用"
      pattern: "var\\(--color-"
---

<objective>
UI 层贴皮：新建 4 个 todo 组件（drawer / list / item / toggle-button），AppLayout 条件式 grid + 客户端化 + persist rehydrate，chat-area 加 header 挂 toggle 按钮，globals.css 追加 `@keyframes todoEnter`（不新增 color/border token，严守 D-15）。

Purpose: TODO-01 / TODO-02 的视觉呈现全在此 plan 完成；auto-open 已在 11-03 闭环，本 plan 只负责"面板打开后内容正确、三态图标符合 DESIGN.md token"。
Output: 前端 UI 完整可见、可折叠、三态图标正确、next build 绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-todo-panel/11-CONTEXT.md
@.planning/phases/11-todo-panel/11-RESEARCH.md
@.planning/phases/11-todo-panel/11-PATTERNS.md
@.planning/phases/11-todo-panel/11-03-SUMMARY.md
@DESIGN.md
@frontend/src/app/globals.css
@frontend/src/components/layout/app-layout.tsx
@frontend/src/components/chat/chat-area.tsx
@frontend/src/components/chat/tool-pill.tsx
@frontend/src/components/chat/message-list.tsx
@frontend/src/components/sidebar/session-item.tsx
@frontend/src/stores/ui-store.ts
@frontend/src/stores/chat-store.ts
@frontend/src/lib/types.ts

<interfaces>
<!-- globals.css 现有 token 清单（globals.css:11-31），仅可引用 NOT 新增 -->
```css
--color-bg-panel: #0f1011;
--color-bg-surface: #191a1b;
--color-accent: #5e6ad2;
--color-border-subtle: rgba(255, 255, 255, 0.05);
--color-border-standard: rgba(255, 255, 255, 0.08);
--color-text-secondary: #d0d6e0;
--color-text-tertiary: #8a8f98;
```

<!-- AppLayout 当前 15 行（app-layout.tsx:1-15）—— 当前是 server component -->
<!-- ChatArea 当前 13 行（chat-area.tsx:1-13）—— 需加 header -->

<!-- Lucide icon 惯例（tool-pill/hitl-card/session-item 三处确认） -->
<!-- <Check size={12} strokeWidth={3} /> / <ListTodo size={14} /> / aria-hidden="true" / 颜色用 CSS 变量 -->

<!-- D-08 三态（CONTEXT.md 已在 11-02 更正 token）： -->
<!-- pending:   空心圆，border: 1.5px solid var(--color-border-standard) -->
<!-- in_progress: 半圈 SVG spinner, stroke var(--color-accent), 1s linear 循环 -->
<!-- completed: 实心圆填 var(--color-accent) + 白色 Check 12px -->

<!-- D-09 动效： -->
<!-- 新 item 进入: opacity 0→1 + translateY 4→0, 200ms ease-out (@keyframes todoEnter) -->
<!-- 状态切换: 图标 150ms ease color/fill 过渡 -->
<!-- 文案颜色恒定 --color-text-secondary -->

<!-- D-01 Drawer 容器 320px 固定，左边线 --color-border-subtle，背景 --color-bg-panel -->
<!-- D-02 toggle 按钮无徽标/红点；drawer 关闭时右上角只有按钮本身 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 4 个 todo 组件 + globals.css 加 @keyframes todoEnter</name>
  <files>
    frontend/src/app/globals.css
    frontend/src/components/todo/todo-item.tsx
    frontend/src/components/todo/todo-list.tsx
    frontend/src/components/todo/todo-drawer.tsx
    frontend/src/components/todo/todo-toggle-button.tsx
  </files>
  <read_first>
    - frontend/src/app/globals.css（完整文件，定位 @keyframes dot-pulse 附近，新 keyframe 追加在同段）
    - frontend/src/components/chat/tool-pill.tsx（三态图标 + CSS 变量色模板）
    - frontend/src/components/chat/message-list.tsx:32-60（空态 + 容器 class 对照）
    - frontend/src/components/sidebar/session-item.tsx:37-47（icon-only button 样式模板）
    - DESIGN.md（颜色 / 字重 / 间距 / 圆角 / 动效时长；硬约束）
    - frontend/src/stores/ui-store.ts（确认 `toggleDrawer` / `closeDrawer` / `todoDrawerOpen` action 签名）
    - frontend/src/stores/chat-store.ts（确认 `todos` state 选择器用法）
  </read_first>
  <behavior>
    globals.css:
    - 追加 `@keyframes todoEnter { from { opacity: 0; transform: translateY(4px); } to { opacity: 1; transform: translateY(0); } }`
    - 不新增任何 `--color-*` / `--font-*` token（D-15 硬约束）

    todo-item.tsx:
    - 渲染 `{todo}: { todo: Todo }` 单条
    - 三态图标（pending/in_progress/completed）按 D-08 token 走（已在 11-02 更正为 --color-border-standard）
    - 文案固定 `text-[var(--color-text-secondary)]` + 15px（text-sm 即 14px 或 text-[15px] 任选贴近 DESIGN.md；优先 text-sm leading-5 与 message-list idle 空态对齐）
    - 容器 `animate-[todoEnter_200ms_ease-out]`
    - 图标切换过渡 `transition-all duration-150`
    - spinner 用 inline SVG（半圈 / 3/4 圈皆可，推荐半圈），`animate-spin` + `animationDuration: "1s"`
    - Check 图标用 `lucide-react` 的 `<Check size={12} strokeWidth={3} />`
    - 不使用 `dangerouslySetInnerHTML`（T-11-02）

    todo-list.tsx:
    - props `{ todos: Todo[] }` 或直接 `useChatStore((s) => s.todos)` 订阅
    - 空态：`<div className="flex flex-1 items-center justify-center text-[15px] text-[var(--color-text-tertiary)]">Agent 尚未制定任务计划</div>`
    - 非空：`{todos.map((t, i) => <TodoItem key={i} todo={t} />)}`（SSE 全量覆盖语义下 index 作 key 可接受）

    todo-drawer.tsx:
    - 顶层容器 `<aside className="flex h-screen flex-col bg-[var(--color-bg-panel)] border-l border-[var(--color-border-subtle)]">`（D-01）
    - 头部：标题 "任务计划" + 右侧关闭按钮（调 `useUIStore.getState().closeDrawer()`）
    - 主体：`<TodoList />`
    - 不做 translateX 动画（Pitfall 7），动画由 AppLayout 的 grid-template-columns 过渡承担

    todo-toggle-button.tsx:
    - icon-only button，图标 `<ListTodo size={14} aria-hidden="true" />`
    - `aria-label="切换任务面板"`
    - `aria-pressed={drawerOpen}`
    - `onClick` 调 `useUIStore.getState().toggleDrawer()`（或顶层 selector 绑定）
    - 样式对照 session-item 删除按钮，但**移除** `opacity-0 group-hover:opacity-100`（按钮常显）
    - 使用 `"use client"` 指令（需读 ui-store）
  </behavior>
  <action>
    **子任务 A —— `frontend/src/app/globals.css` 追加 @keyframes：**

    在已有 `@keyframes dot-pulse` 同一段尾部（文件末尾）追加：

    ```css
    @keyframes todoEnter {
      from { opacity: 0; transform: translateY(4px); }
      to   { opacity: 1; transform: translateY(0); }
    }
    ```

    **硬约束**：不新增任何 `--color-*` / `--font-*` token（D-15 硬约束）。仅新 keyframe。

    **子任务 B —— 新建 `frontend/src/components/todo/todo-item.tsx`：**

    按 PATTERNS.md §todo-item.tsx 示例 + D-08 token 修正值（`--color-border-standard`）：

    ```tsx
    import { Check } from "lucide-react";
    import type { Todo } from "@/lib/types";

    export function TodoItem({ todo }: { todo: Todo }) {
      return (
        <div className="flex items-start gap-3 py-2 px-4 animate-[todoEnter_200ms_ease-out]">
          <span className="shrink-0 mt-0.5">
            {todo.status === "pending" && <PendingCircle />}
            {todo.status === "in_progress" && <InProgressSpinner />}
            {todo.status === "completed" && <CompletedCircle />}
          </span>
          <span className="text-[var(--color-text-secondary)] text-sm leading-5 transition-colors duration-150">
            {todo.content}
          </span>
        </div>
      );
    }

    function PendingCircle() {
      return (
        <span
          aria-hidden="true"
          className="inline-block w-4 h-4 rounded-full transition-all duration-150"
          style={{ border: "1.5px solid var(--color-border-standard)" }}
        />
      );
    }

    function InProgressSpinner() {
      return (
        <svg
          aria-hidden="true"
          className="w-4 h-4 animate-spin"
          style={{ animationDuration: "1s" }}
          viewBox="0 0 16 16"
        >
          <circle cx="8" cy="8" r="6.5" fill="none" stroke="var(--color-border-standard)" strokeWidth="1.5" />
          <path
            d="M 8 1.5 A 6.5 6.5 0 0 1 14.5 8"
            fill="none"
            stroke="var(--color-accent)"
            strokeWidth="1.5"
            strokeLinecap="round"
          />
        </svg>
      );
    }

    function CompletedCircle() {
      return (
        <span
          aria-hidden="true"
          className="inline-flex w-4 h-4 rounded-full items-center justify-center transition-all duration-150"
          style={{ backgroundColor: "var(--color-accent)" }}
        >
          <Check size={12} strokeWidth={3} className="text-white" />
        </span>
      );
    }
    ```

    **硬约束**：
    - 文案 `text-[var(--color-text-secondary)]` 不变色（D-08）
    - 图标尺寸 16px（外层 `w-4 h-4`），Check 内部 12px
    - 所有颜色走 CSS 变量，不写字面 `#5e6ad2`

    **子任务 C —— 新建 `frontend/src/components/todo/todo-list.tsx`：**

    ```tsx
    "use client";
    import { useChatStore } from "@/stores/chat-store";
    import { TodoItem } from "./todo-item";

    export function TodoList() {
      const todos = useChatStore((s) => s.todos);

      if (todos.length === 0) {
        return (
          <div className="flex flex-1 items-center justify-center text-[15px] text-[var(--color-text-tertiary)]">
            Agent 尚未制定任务计划
          </div>
        );
      }

      return (
        <div className="flex-1 overflow-y-auto py-2">
          {todos.map((t, i) => (
            <TodoItem key={i} todo={t} />
          ))}
        </div>
      );
    }
    ```

    **子任务 D —— 新建 `frontend/src/components/todo/todo-drawer.tsx`：**

    ```tsx
    "use client";
    import { X } from "lucide-react";
    import { useUIStore } from "@/stores/ui-store";
    import { TodoList } from "./todo-list";

    export function TodoDrawer() {
      const closeDrawer = useUIStore((s) => s.closeDrawer);

      return (
        <aside className="flex h-screen flex-col bg-[var(--color-bg-panel)] border-l border-[var(--color-border-subtle)]">
          <header className="flex items-center justify-between px-4 py-3 border-b border-[var(--color-border-subtle)]">
            <span className="text-[13px] font-medium text-[var(--color-text-secondary)]">任务计划</span>
            <button
              type="button"
              aria-label="关闭任务面板"
              onClick={closeDrawer}
              className="p-1 rounded hover:bg-[rgba(255,255,255,0.08)] transition-colors"
            >
              <X size={14} aria-hidden="true" className="text-[var(--color-text-tertiary)]" />
            </button>
          </header>
          <TodoList />
        </aside>
      );
    }
    ```

    **子任务 E —— 新建 `frontend/src/components/todo/todo-toggle-button.tsx`：**

    ```tsx
    "use client";
    import { ListTodo } from "lucide-react";
    import { useUIStore } from "@/stores/ui-store";

    export function TodoToggleButton() {
      const toggleDrawer = useUIStore((s) => s.toggleDrawer);
      const drawerOpen = useUIStore((s) => s.todoDrawerOpen);

      return (
        <button
          type="button"
          aria-label="切换任务面板"
          aria-pressed={drawerOpen}
          onClick={toggleDrawer}
          className="p-1.5 rounded hover:bg-[rgba(255,255,255,0.08)] transition-colors"
        >
          <ListTodo size={14} aria-hidden="true" className="text-[var(--color-text-tertiary)]" />
        </button>
      );
    }
    ```

    **外科手术原则：**
    - 不改 session-item / tool-pill / message-list / hitl-card 任何现有组件
    - 不新增任何 `--color-*` token
    - 不引入新的图标库或依赖
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - [ ] globals.css 新增 keyframe：`grep -n "@keyframes todoEnter" frontend/src/app/globals.css` 返回 1 行
    - [ ] globals.css 未新增 color/border token：`grep -cE "^ *--color-[a-z-]+:|^ *--font-[a-z-]+:" frontend/src/app/globals.css` 数值与改前相同（Executor 记下改前数，diff 核对）
    - [ ] 四个组件文件存在：`ls frontend/src/components/todo/{todo-item,todo-list,todo-drawer,todo-toggle-button}.tsx` 全部命中
    - [ ] todo-item 三态条件渲染：`grep -c "status === \"pending\"\|status === \"in_progress\"\|status === \"completed\"" frontend/src/components/todo/todo-item.tsx` ≥ 3
    - [ ] todo-item 引用正确 border token：`grep -n "color-border-standard" frontend/src/components/todo/todo-item.tsx` ≥ 1；`grep -c "color-border-default" frontend/src/components/todo/todo-item.tsx` = 0
    - [ ] 文案颜色固定 secondary：`grep -n "color-text-secondary" frontend/src/components/todo/todo-item.tsx` ≥ 1
    - [ ] 无 XSS 风险：`grep -rc "dangerouslySetInnerHTML" frontend/src/components/todo/` 全部返回 0
    - [ ] toggle-button 有 aria-pressed：`grep -n "aria-pressed" frontend/src/components/todo/todo-toggle-button.tsx` 返回 1
    - [ ] toggle-button 常显（无 group-hover 隐藏）：`grep -c "opacity-0 group-hover" frontend/src/components/todo/todo-toggle-button.tsx` = 0
    - [ ] drawer 左边线 subtle + panel 背景：`grep -E "border-l border-\[var\(--color-border-subtle\)\]" frontend/src/components/todo/todo-drawer.tsx` ≥ 1
    - [ ] next build 通过：`cd frontend && npm run build`
  </acceptance_criteria>
  <done>4 个 todo 组件齐备 + keyframe 就位 + next build 绿；UI 素材齐全等待装配。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chat-area 加 header 挂 TodoToggleButton + AppLayout 条件式 grid + use client + rehydrate</name>
  <files>
    frontend/src/components/chat/chat-area.tsx
    frontend/src/components/layout/app-layout.tsx
  </files>
  <read_first>
    - frontend/src/components/chat/chat-area.tsx（当前 13 行原文，确认 section 结构）
    - frontend/src/components/layout/app-layout.tsx（当前 15 行原文，确认 grid 结构）
    - frontend/src/stores/ui-store.ts（11-03 产物，确认 `persist.rehydrate()` 调用方式）
    - frontend/src/components/todo/todo-drawer.tsx（11-04 Task 1 产物）
    - frontend/src/components/todo/todo-toggle-button.tsx（11-04 Task 1 产物）
  </read_first>
  <behavior>
    chat-area.tsx:
    - 保持 `<section>` 根容器 class 不变
    - 加 `<header>` 在 section 第一个子元素位置（在 `{children}` 之前）
    - header 右上对齐，包含 `<TodoToggleButton />`
    - 标记 `"use client"`（header 内 TodoToggleButton 需订阅 ui-store）
    - 支持 children（原语义不变）

    app-layout.tsx:
    - 顶部加 `"use client"` 指令（原为 server component，需订阅 ui-store）
    - 顶层 `useEffect(() => { void useUIStore.persist.rehydrate(); }, [])`
    - 读 `useUIStore((s) => s.todoDrawerOpen)`
    - grid 类条件化：`drawerOpen ? "grid-cols-[240px_1fr_320px]" : "grid-cols-[240px_1fr]"`
    - 外层 div 加 `transition-[grid-template-columns] duration-200 ease-out`（D-09 + Pitfall 7）
    - `drawerOpen && <TodoDrawer />` 挂在第三列
    - 保持 sidebar / children 结构
    - 保持 `border-l border-[var(--color-border-subtle)]`（children 外层既有左边线）
  </behavior>
  <action>
    **子任务 A —— 改 `frontend/src/components/chat/chat-area.tsx`：**

    ```tsx
    "use client";
    import type { ReactNode } from "react";
    import { TodoToggleButton } from "@/components/todo/todo-toggle-button";

    type ChatAreaProps = {
      children: ReactNode;
    };

    export function ChatArea({ children }: ChatAreaProps) {
      return (
        <section className="flex h-screen flex-col bg-[var(--color-bg-panel)]">
          <header className="flex items-center justify-end px-4 py-2 border-b border-[var(--color-border-subtle)]">
            <TodoToggleButton />
          </header>
          {children}
        </section>
      );
    }
    ```

    **⚠️ 注意：** header 使用 `border-b border-[var(--color-border-subtle)]` 保持与 drawer header 一致；`justify-end` 让按钮右上角对齐（D-02）。`px-4 py-2` 轻量；高度不设死值（内容自适应）。

    **子任务 B —— 改 `frontend/src/components/layout/app-layout.tsx`：**

    ```tsx
    "use client";
    import { useEffect, type ReactNode } from "react";
    import { useUIStore } from "@/stores/ui-store";
    import { TodoDrawer } from "@/components/todo/todo-drawer";

    type AppLayoutProps = {
      children: ReactNode;
      sidebar: ReactNode;
    };

    export function AppLayout({ children, sidebar }: AppLayoutProps) {
      const drawerOpen = useUIStore((s) => s.todoDrawerOpen);

      useEffect(() => {
        void useUIStore.persist.rehydrate();
      }, []);

      const cols = drawerOpen
        ? "grid-cols-[240px_1fr_320px]"
        : "grid-cols-[240px_1fr]";

      return (
        <div
          className={`grid min-h-screen ${cols} bg-[var(--color-bg-panel)] text-[var(--color-text-primary)] transition-[grid-template-columns] duration-200 ease-out`}
        >
          {sidebar}
          <div className="border-l border-[var(--color-border-subtle)]">{children}</div>
          {drawerOpen && <TodoDrawer />}
        </div>
      );
    }
    ```

    **外科手术原则：**
    - chat-area：只加 header + use client + toggle button import，section 主体语义不变
    - app-layout：只加 use client + rehydrate + grid 条件 + TodoDrawer 挂载；不动 sidebar 渲染顺序与 children 的 border-l

    **不得做：**
    - 不把 drawer 本体做 translateX 动画（Pitfall 7）
    - 不在 page.tsx 做 rehydrate（统一在 app-layout 顶层调一次）
    - 不暴露"强制打开 drawer"的新 API
  </action>
  <verify>
    <automated>cd frontend && npm run test -- run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - [ ] chat-area 加了 `"use client"`：`grep -n "^\"use client\"" frontend/src/components/chat/chat-area.tsx` 返回 1 行
    - [ ] chat-area 有 header：`grep -c "<header" frontend/src/components/chat/chat-area.tsx` ≥ 1
    - [ ] chat-area 挂了 TodoToggleButton：`grep -n "TodoToggleButton" frontend/src/components/chat/chat-area.tsx` ≥ 2（import + JSX）
    - [ ] app-layout 加了 `"use client"`：`grep -n "^\"use client\"" frontend/src/components/layout/app-layout.tsx` 返回 1 行
    - [ ] app-layout 调 rehydrate：`grep -n "persist.rehydrate" frontend/src/components/layout/app-layout.tsx` 返回 1 行
    - [ ] app-layout 订阅 todoDrawerOpen：`grep -n "todoDrawerOpen" frontend/src/components/layout/app-layout.tsx` 返回 1 行
    - [ ] app-layout 条件式 grid：`grep -c "grid-cols-\[240px_1fr_320px\]\|grid-cols-\[240px_1fr\]" frontend/src/components/layout/app-layout.tsx` ≥ 2
    - [ ] app-layout 有 transition：`grep -n "transition-\[grid-template-columns\]" frontend/src/components/layout/app-layout.tsx` 返回 1 行
    - [ ] 挂载 TodoDrawer：`grep -n "TodoDrawer" frontend/src/components/layout/app-layout.tsx` ≥ 2（import + 条件渲染）
    - [ ] 既有 `border-l border-[var(--color-border-subtle)]`（children 外层）保留：`grep -n "border-l border-\[var(--color-border-subtle)\]" frontend/src/components/layout/app-layout.tsx` ≥ 1
    - [ ] vitest + next build 全绿：`cd frontend && npm run test -- run && npm run build`
    - [ ] auto-open 依然是 SSE-only（未被 app-layout 的 rehydrate 污染）：`grep -n "autoOpenDrawer" frontend/src/components/layout/app-layout.tsx` = 0
  </acceptance_criteria>
  <done>AppLayout 条件式 grid + rehydrate 就绪；chat 顶栏 toggle 按钮可用；点击按钮能看到 drawer 弹出并显示 todos。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → DOM rendering | TodoItem 渲染 todo.content 为 React children；content 源自后端 state（Agent 生成） |
| Browser → localStorage | AppLayout rehydrate 读 `neuron-assistant:ui-store:v1`（只含 `todoDrawerOpen: boolean`） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | T (Tampering / XSS via injected todo content) | todo-item.tsx | mitigate | todo.content 仅通过 React 文本节点渲染（`{todo.content}` 在 `<span>` 内），React 默认 escape；本 plan 禁止 `dangerouslySetInnerHTML`—— 静态审查：`grep -rc "dangerouslySetInnerHTML" frontend/src/components/todo/` 必须全为 0（AC 内已锁） |
| T-11-05 | D (Denial via hydration mismatch on refresh) | app-layout.tsx persist 初始化 | mitigate | `skipHydration: true`（ui-store 已在 11-03 配置）+ 顶层 useEffect rehydrate；首渲染用 store 初值（false），避免 SSR 与客户端 mismatch 导致的控制台错误与首帧闪烁（RESEARCH.md Pitfall 1） |
| T-11-06 | I (Info disclosure via drawer state leakage) | localStorage | accept | 仅 persist `todoDrawerOpen: boolean`，无 PII、无 sessionId、无 token；非敏感；持久化作为 UX 偏好，可接受 |
</threat_model>

<verification>
```bash
# 完整 build + test
cd frontend && npm run test -- run && npm run build

# DESIGN.md token 合规：不新增
grep -cE "^ *--color-[a-z-]+:|^ *--font-[a-z-]+:" frontend/src/app/globals.css   # 数值应与改前一致

# XSS 防护
grep -rc "dangerouslySetInnerHTML" frontend/src/components/todo/   # 应全为 0

# auto-open 不被污染（只在 use-sse）
grep -rn "autoOpenDrawer" frontend/src/
# 期望命中：ui-store.ts 定义、use-sse.ts 调用、两个 ui-store test；
# 禁止命中：app-layout.tsx / chat-area.tsx / todo-* 组件
```
</verification>

<success_criteria>
- 4 个 todo 组件创建完毕，样式引用 DESIGN.md token（不新增）
- globals.css `@keyframes todoEnter` 就位
- AppLayout 客户端化 + 条件式 grid + rehydrate 齐全
- Chat header 挂 toggle 按钮，无红点/徽标
- vitest + next build 全绿
- 无 XSS 入口（grep 审查为 0）
- auto-open 触发源仍只在 use-sse.ts
</success_criteria>

<output>
After completion, create `.planning/phases/11-todo-panel/11-04-SUMMARY.md` with:
- 4 个新组件的文件位置 + 行数
- AppLayout / ChatArea 改动 diff 摘要
- 三态图标 token 对照表（pending/in_progress/completed 各用哪个变量）
- 测试 + build 绿屏证据
</output>
