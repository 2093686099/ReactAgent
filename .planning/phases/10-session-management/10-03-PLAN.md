---
phase: 10-session-management
plan: 03
type: execute
wave: 1
depends_on:
  - 10-01
files_modified:
  - frontend/src/lib/types.ts
  - frontend/src/lib/time-group.ts
  - frontend/src/lib/api.ts
  - frontend/src/stores/session-store.ts
  - frontend/src/stores/chat-store.ts
  - frontend/src/hooks/use-sse.ts
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
  - CHAT-08
user_setup: []

must_haves:
  truths:
    - "types.ts 导出 Session 接口（id / title / created_at / last_updated / status / last_task_id?）"
    - "api.ts 导出 listSessions / deleteSession / createSessionAPI / loadHistory 四个 fetch 函数"
    - "session-store.ts 独立存在，持有 sessions / activeSessionId / deletedPending / loadSessions / setActive / createLocal / deleteOptimistic / restoreSession"
    - "chat-store.ts 中 activeSessionId 被移除，新增 loadHistory(messages) action（一次性 set，不走 token buffer）"
    - "useSSE 签名改为 useSSE(taskId, sessionId)，effect 依赖数组包含 sessionId"
    - "chat-store.reset 不再生成新 activeSessionId（职责已迁走）"
  artifacts:
    - path: "frontend/src/lib/types.ts"
      provides: "Session 接口 + ActiveTask 类型 + HistoryResponse 类型"
      contains: "export interface Session"
    - path: "frontend/src/lib/api.ts"
      provides: "listSessions / deleteSession / createSessionAPI / loadHistory"
      contains: "listSessions"
    - path: "frontend/src/stores/session-store.ts"
      provides: "domain-sliced session store（与 chat-store 并列）"
      contains: "useSessionStore"
    - path: "frontend/src/hooks/use-sse.ts"
      provides: "useSSE(taskId, sessionId)"
      contains: "sessionId"
  key_links:
    - from: "frontend/src/hooks/use-sse.ts useEffect deps"
      to: "sessionId 参数"
      via: "依赖数组包含 sessionId 以触发 cleanup"
      pattern: "sessionId,"
    - from: "frontend/src/stores/session-store.ts"
      to: "frontend/src/lib/api.ts::listSessions"
      via: "loadSessions action 调用"
      pattern: "listSessions"
    - from: "frontend/src/stores/chat-store.ts::loadHistory"
      to: "messages 一次性 set（不走 RAF token buffer）"
      via: "set({ messages })"
      pattern: "loadHistory"
---

<objective>
Phase 10 Wave 1 前端数据层。依赖 Plan 01 已冻结的后端 API 契约，新增 `session-store`（domain-sliced）、从 `chat-store` 迁走 `activeSessionId`、新增 `chat-store.loadHistory`、为 `useSSE` hook 加 `sessionId` 参数（依赖数组更新，触发 cleanup）。本 Plan 不触碰 UI 组件与 page.tsx 组合层（留给 Plan 04）。

Purpose: UI 层（sidebar 真实列表 + page.tsx handleSwitch/handleDelete/handleNew）需要一个干净的数据层底座。本 Plan 完成"数据骨架"，Plan 04 只需做组合与视觉装配。
Output: 所有 store / hook / api / types 就位；build 通过；chat-store 的现有测试手验（发消息 / HITL）不破。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-session-management/10-CONTEXT.md
@.planning/phases/10-session-management/10-RESEARCH.md

@frontend/src/lib/types.ts
@frontend/src/lib/api.ts
@frontend/src/stores/chat-store.ts
@frontend/src/hooks/use-sse.ts
@frontend/src/lib/time-group.ts

<interfaces>
<!-- Plan 01 已冻结的后端 API 契约，Plan 03 按此实现 fetch 包装 -->

```
GET  /api/sessions
  → { sessions: Session[] }      // Session: {id? or session_id? 注意字段名} 详见下方 note

GET  /api/sessions/{id}/messages
  → {
      messages: Message[],       // Segment[] 已被后端 messages_to_segments 转换
      active_task: { task_id: string; status: string } | null,
      truncate_after_active_task: boolean,
    }

POST /api/sessions
  body: { session_id?: string; title?: string }    // 都可省略
  → { session_id: string; title: string }

DELETE /api/sessions/{id}
  → { status: "success" }
```

**字段名 note（非常重要 — 避免前后端字段名不一致）**：后端 `session_svc.list_sessions` 返回的 JSON 使用 `session_id`（不是 `id`），字段来自 Redis JSON schema 见 `backend/app/services/session.py:44-50`。前端可选两种做法：
- (A) 保持后端字段名 `session_id`，types.ts 的 Session 接口用 `session_id`；
- (B) 在 api.ts::listSessions 做字段映射 `{session_id → id}`，types.ts 用 `id`。

**本 Plan 采用 (B)**（契合已有 time-group.ts 期望的 `s.id`，避免 sidebar/time-group 都要改）。api.ts::listSessions 负责映射。

<!-- 现有 chat-store 关键代码（需要改动的位置）-->
```typescript
// frontend/src/stores/chat-store.ts
type ChatState = {
  messages: Message[];
  status: ChatStatus;
  currentTaskId: string | null;
  activeSessionId: string;                 // ← 本 Plan 移除
  errorMessage: string | null;
  // ... actions
  reset: () => void;                       // ← 本 Plan 改：reset 不生成新 activeSessionId
}

// reset 当前实现（line 307-320）会 createSessionId()，需移除该副作用
reset: () => {
  // ... clear tokenBuffer
  set({ messages: [], status: "idle", currentTaskId: null, errorMessage: null,
        activeSessionId: createSessionId() });   // ← 删掉最后一行
}
```

<!-- 现有 useSSE 签名（需要改动）-->
```typescript
export function useSSE(taskId: string | null): void { ... }
// 依赖数组当前为 [taskId, appendToken, addToolSegment, updateToolSegment, addHitlSegment, finishMessage, setError, setStatus]
// 本 Plan 改：signature 加 sessionId: string，依赖数组加 sessionId
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: types.ts 新增 Session + ActiveTask；api.ts 新增 4 个 fetch 包装；time-group.ts 切换 import</name>
  <files>
    frontend/src/lib/types.ts
    frontend/src/lib/api.ts
    frontend/src/lib/time-group.ts
  </files>
  <read_first>
    - frontend/src/lib/types.ts（现有 Message / Segment / InvokeResponse）
    - frontend/src/lib/api.ts（现有 invokeChat / resumeChat 风格）
    - frontend/src/lib/time-group.ts（Plan 02 落地，若存在局部 type Session 需迁走）
    - .planning/phases/10-session-management/10-01-PLAN.md（后端 API schema 契约）
  </read_first>
  <action>
    1. `frontend/src/lib/types.ts` 末尾追加：
       ```typescript
       export interface Session {
         id: string;
         title: string;
         created_at: number;
         last_updated: number;   // 后端秒级 float
         status: string;         // "idle" | "running" | ...
         last_task_id?: string | null;
       }

       export interface ActiveTask {
         task_id: string;
         status: string;
       }

       export interface HistoryResponse {
         messages: Message[];
         active_task: ActiveTask | null;
         truncate_after_active_task: boolean;
       }
       ```
       不改现有导出，不改 Message 形状。
    2. `frontend/src/lib/api.ts` 追加四个函数（保留现有 invokeChat / resumeChat）：
       ```typescript
       import type { Session, HistoryResponse } from "@/lib/types";

       type RawSession = { session_id: string; title?: string; created_at: number;
         last_updated: number; status: string; last_task_id?: string | null };

       function mapSession(raw: RawSession): Session {
         return {
           id: raw.session_id,
           title: raw.title ?? "",
           created_at: raw.created_at,
           last_updated: raw.last_updated,
           status: raw.status,
           last_task_id: raw.last_task_id ?? null,
         };
       }

       export async function listSessions(): Promise<Session[]> {
         const r = await fetch(`${API_BASE}/api/sessions`);
         if (!r.ok) throw new Error(`HTTP ${r.status}`);
         const body = (await r.json()) as { sessions: RawSession[] };
         return body.sessions.map(mapSession);
       }

       export async function createSessionAPI(input: { session_id?: string; title?: string } = {}): Promise<Session> {
         const r = await fetch(`${API_BASE}/api/sessions`, {
           method: "POST",
           headers: { "Content-Type": "application/json" },
           body: JSON.stringify(input),
         });
         if (!r.ok) throw new Error(`HTTP ${r.status}`);
         const body = (await r.json()) as RawSession & { session_id: string };
         return mapSession({ ...body, created_at: body.created_at ?? 0, last_updated: body.last_updated ?? 0, status: body.status ?? "idle" });
       }

       export async function deleteSession(sessionId: string): Promise<void> {
         const r = await fetch(`${API_BASE}/api/sessions/${sessionId}`, { method: "DELETE" });
         if (!r.ok) throw new Error(`HTTP ${r.status}`);
       }

       export async function loadHistory(sessionId: string): Promise<HistoryResponse> {
         const r = await fetch(`${API_BASE}/api/sessions/${sessionId}/messages`);
         if (!r.ok) throw new Error(`HTTP ${r.status}`);
         return (await r.json()) as HistoryResponse;
       }
       ```
    3. `frontend/src/lib/time-group.ts`：若 Plan 02 在文件顶部用了局部 `type Session = ...`，把它删掉，改为 `import type { Session } from "@/lib/types";`（types.ts 已有）。保持 `groupSessions` 行为不变。
    4. 运行 `cd frontend && npm run test -- run` 确认 time-group.test.ts 仍全绿。运行 `cd frontend && npx tsc --noEmit` 确认类型无错。
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run test -- run</automated>
  </verify>
  <done>
    tsc 无 error；vitest 全绿；`grep -n "export interface Session" frontend/src/lib/types.ts` 命中；`grep -n "listSessions\|deleteSession\|createSessionAPI\|loadHistory" frontend/src/lib/api.ts | wc -l` ≥ 4；time-group.ts 顶部没有局部 `type Session`。
  </done>
</task>

<task type="auto">
  <name>Task 2: session-store.ts 新建 + chat-store.ts 移除 activeSessionId + 新增 loadHistory</name>
  <files>
    frontend/src/stores/session-store.ts
    frontend/src/stores/chat-store.ts
  </files>
  <read_first>
    - frontend/src/stores/chat-store.ts（全文；注意 reset 在 line 307-320 生成新 activeSessionId）
    - frontend/src/lib/api.ts（Task 1 新增的函数）
    - .planning/phases/10-session-management/10-CONTEXT.md §D-14..D-16, §P-04, §P-07
    - .planning/phases/10-session-management/10-RESEARCH.md §Pattern 3（session-store 完整示例）+ §Anti-Patterns
  </read_first>
  <action>
    1. 新建 `frontend/src/stores/session-store.ts`：
       ```typescript
       import { create } from "zustand";
       import type { Session } from "@/lib/types";
       import { listSessions, deleteSession, createSessionAPI } from "@/lib/api";

       function newLocalId(): string {
         if (typeof crypto !== "undefined" && "randomUUID" in crypto) return crypto.randomUUID();
         return `session-${Date.now()}`;
       }

       type SessionState = {
         sessions: Session[];
         activeSessionId: string;          // 永远非空（首次进入页面 createLocal 兜底）
         deletedPending: Session | null;   // 最近一次删除（8s 内可撤销）
         loadSessions: () => Promise<Session[]>;
         setActive: (id: string) => void;
         createLocal: () => string;                            // 生成本地 uuid + 加入 sessions（占位，首次 invoke 时后端幂等持久化）
         deleteOptimistic: (id: string) => Promise<void>;      // 乐观删除（先调后端，然后从列表移除，并置 deletedPending）
         restoreSession: (session: Session) => Promise<void>;  // 撤销：POST /api/sessions 幂等恢复
         clearDeletedPending: () => void;
         upsertSession: (session: Session) => void;            // invoke 后用后端返回/列表返回的真实 session 覆盖本地占位
       };

       export const useSessionStore = create<SessionState>((set, get) => ({
         sessions: [],
         activeSessionId: newLocalId(),
         deletedPending: null,

         loadSessions: async () => {
           const sessions = await listSessions();
           set({ sessions });
           return sessions;
         },

         setActive: (id) => set({ activeSessionId: id }),

         createLocal: () => {
           const id = newLocalId();
           // 本地占位 session（last_updated=now 秒级）—— 进入 today 分组
           const placeholder: Session = {
             id, title: "", created_at: Date.now() / 1000,
             last_updated: Date.now() / 1000, status: "idle", last_task_id: null,
           };
           set((s) => ({ sessions: [placeholder, ...s.sessions], activeSessionId: id }));
           return id;
         },

         deleteOptimistic: async (id) => {
           const before = get().sessions;
           const target = before.find((s) => s.id === id) ?? null;
           set({ sessions: before.filter((s) => s.id !== id), deletedPending: target });
           try {
             await deleteSession(id);
           } catch (e) {
             // 失败 → 回滚
             set({ sessions: before, deletedPending: null });
             throw e;
           }
         },

         restoreSession: async (session) => {
           // 幂等 POST —— 后端若存在则直接返回现有，若不存在则恢复占位
           try {
             await createSessionAPI({ session_id: session.id, title: session.title });
           } catch {
             // 吞掉错误（8s 窗口过短 & 撤销失败不需要重试）；交 UI 决定提示
           }
           set((s) => ({
             sessions: [session, ...s.sessions].sort((a, b) => b.last_updated - a.last_updated),
             deletedPending: null,
           }));
         },

         clearDeletedPending: () => set({ deletedPending: null }),

         upsertSession: (session) => set((s) => {
           const idx = s.sessions.findIndex((x) => x.id === session.id);
           if (idx === -1) return { sessions: [session, ...s.sessions] };
           const next = [...s.sessions];
           next[idx] = session;
           return { sessions: next };
         }),
       }));
       ```
    2. `frontend/src/stores/chat-store.ts`：
       - Type 定义 `ChatState` 移除 `activeSessionId: string;` 字段与对应的 action（没有现成 setter，仅 initial + reset 生成）。
       - 移除文件顶部 `createSessionId` 函数（**仅当** 除 chat-store 外无其他使用者；`grep -n "createSessionId\|from \"@/stores/chat-store\"" frontend/src/` 确认）。如果 page.tsx 等还在 import 则保留但改为 unused 并在 Plan 04 Task 2 清掉 —— 本 Plan 安全做法：**保留 `createSessionId` 函数定义但不再在 store 内调用**，以免跨 Plan 破坏 page.tsx import。
       - Store initial state 里移除 `activeSessionId: createSessionId(),`。
       - `reset` action：移除 `activeSessionId: createSessionId()` 那一行；保留 messages/status/currentTaskId/errorMessage 四字段的清空。
       - **新增 action `loadHistory: (messages: Message[]) => void`**：
         ```typescript
         loadHistory: (messages) => {
           if (rafId) { cancelAnimationFrame(rafId); rafId = 0; }
           tokenBuffer = "";
           set({ messages, status: "idle", currentTaskId: null, errorMessage: null });
         },
         ```
         同时在 `ChatState` 类型声明里加 `loadHistory: (messages: Message[]) => void;`。
       - **关键**：`loadHistory` 一次性 set，**不** 走 addAssistantMessage / appendToken（RAF token buffer）。Anti-Pattern §RESEARCH 明确禁止。
    3. 验证：
       - `cd frontend && npx tsc --noEmit` 无 error。
       - `grep -n "activeSessionId" frontend/src/stores/chat-store.ts` **0 命中**（全清除）。
       - `grep -n "activeSessionId" frontend/src/stores/session-store.ts` ≥2 命中（state 字段 + action）。
       - `grep -n "loadHistory" frontend/src/stores/chat-store.ts` ≥1 命中。
    4. **提示下游 Plan 04**：page.tsx 现在会 TS 报错（`useChatStore((state) => state.activeSessionId)` 已不存在），这是 **预期** —— 由 Plan 04 Task 2 统一修复。本 Plan 只做数据层，允许 page.tsx 暂时编译失败；`next build` 会 fail，这是 expected handoff。
  </action>
  <verify>
    <automated>grep -c "activeSessionId" frontend/src/stores/chat-store.ts | xargs -I{} test {} -eq 0 && grep -c "loadHistory" frontend/src/stores/chat-store.ts | xargs -I{} test {} -ge 1 && grep -c "useSessionStore" frontend/src/stores/session-store.ts | xargs -I{} test {} -ge 1 && echo PASS</automated>
  </verify>
  <done>
    session-store.ts 存在且导出 useSessionStore；chat-store.ts 零引用 activeSessionId；chat-store 新增 loadHistory action；vitest 全绿（没回归纯函数测试）；tsc 对 stores/\* 子目录无 error（允许 page.tsx 暂时 TS 报错等 Plan 04 修复）。
  </done>
</task>

<task type="auto">
  <name>Task 3: useSSE 签名加 sessionId + 依赖数组加 sessionId</name>
  <files>
    frontend/src/hooks/use-sse.ts
  </files>
  <read_first>
    - frontend/src/hooks/use-sse.ts（全文；当前 signature `useSSE(taskId: string | null)` / 依赖数组 line 125-134）
    - .planning/phases/10-session-management/10-CONTEXT.md §D-08, §D-09
    - .planning/phases/10-session-management/10-RESEARCH.md §Pattern 4 + §Anti-Patterns（"在 useSSE effect 里根据 activeSessionId 主动 eventSource.close()" 禁止）
  </read_first>
  <action>
    1. 修改签名为：
       ```typescript
       export function useSSE(taskId: string | null, sessionId: string): void {
         // ... 其他 selector 保持不变（Zustand action 选择器返回稳定引用，不需要 useCallback）
       ```
    2. 不要在 effect 里主动 `close()`（Anti-Pattern 禁止）。仅把 `sessionId` 加入依赖数组：
       ```typescript
       }, [
         taskId,
         sessionId,              // 新增
         appendToken,
         addToolSegment,
         updateToolSegment,
         addHitlSegment,
         finishMessage,
         setError,
         setStatus,
       ]);
       ```
    3. 不改 effect 内部任何业务逻辑（token / tool / hitl / error / done 事件处理保持原样）。
    4. 验证：
       - `cd frontend && npx tsc --noEmit` —— 允许 page.tsx 报错（它仍用旧签名 `useSSE(currentTaskId)`），但 use-sse.ts 本体 tsc 无错。
       - `grep -n "sessionId" frontend/src/hooks/use-sse.ts` 至少 2 命中（参数 + 依赖数组）。
  </action>
  <verify>
    <automated>grep -c "sessionId" frontend/src/hooks/use-sse.ts | xargs -I{} test {} -ge 2 && grep -c "sessionId: string" frontend/src/hooks/use-sse.ts | xargs -I{} test {} -ge 1 && echo PASS</automated>
  </verify>
  <done>
    useSSE 签名为 `useSSE(taskId: string | null, sessionId: string)`；依赖数组含 `sessionId`；effect 内部业务逻辑未被改动（可用 `git diff frontend/src/hooks/use-sse.ts` 查看：改动 ≤5 行）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 前端 → 后端 fetch | listSessions / loadHistory 不带显式 user_id，后端 `get_current_user` 目前返回 `settings.default_user_id`（单用户开发环境）；T-10-01/02 的实际 enforcement 由 Plan 01 后端 `user_id=user_id` 检查负责 |
| Zustand store 内状态 | `deletedPending` 只在前端内存；刷新页面即丢失（与 P-07 撤销 8s 窗口语义一致） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | I (Information Disclosure) | `loadHistory(sessionId)` | mitigate | 后端 Plan 01 Task 3 已实施 session_exists(user_id) 校验 + 跨用户测试；前端只做数据获取，不需额外防御 |
| T-10-02 | E (Elevation of Privilege) | `createSessionAPI({session_id})` | mitigate | 同上 —— 后端 create_session 绑定 user_id；前端传任意 session_id 不会拿到别人的会话 |
| T-10-06 | T (Tampering) | `mapSession` 字段映射 | accept | 纯 JSON field rename；无用户输入执行路径 |
</threat_model>

<verification>
```bash
cd frontend && npx tsc --noEmit 2>&1 | grep -v "src/app/page.tsx" | grep -v "src/components/sidebar/sidebar.tsx" | grep -E "error TS"
# expect: 空输出（page.tsx 和 sidebar.tsx 的 TS 报错由 Plan 04 修复，是 expected handoff）

cd frontend && npm run test -- run
# expect: 7 tests passed（time-group 不受本 Plan 影响）
```
</verification>

<success_criteria>
- [x] types.ts 导出 Session / ActiveTask / HistoryResponse
- [x] api.ts 4 个新函数齐备（listSessions / deleteSession / createSessionAPI / loadHistory）
- [x] session-store.ts 导出 useSessionStore，含 sessions / activeSessionId / deletedPending + 6 个 actions
- [x] chat-store.ts 零 activeSessionId 引用；loadHistory action 存在
- [x] useSSE 双参数签名 + 依赖数组加 sessionId
- [x] 除 page.tsx / sidebar.tsx 外，tsc 无 error（后者由 Plan 04 修复）
- [x] vitest time-group 测试不回归
</success_criteria>

<output>
完成后创建 `.planning/phases/10-session-management/10-03-SUMMARY.md`，记录：
- session-store 完整 shape + action 列表
- chat-store diff（activeSessionId 移除 + loadHistory 新增）
- useSSE 签名变更前后对比
- 留给 Plan 04 的遗留点清单（预期 TS 报错位置 + page.tsx / sidebar.tsx 修复要点）
</output>
