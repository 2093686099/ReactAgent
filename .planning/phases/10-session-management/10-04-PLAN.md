---
phase: 10-session-management
plan: 04
type: execute
wave: 2
depends_on:
  - 10-02
  - 10-03
files_modified:
  - frontend/src/components/sidebar/sidebar.tsx
  - frontend/src/components/sidebar/session-item.tsx
  - frontend/src/components/sidebar/session-group.tsx
  - frontend/src/app/page.tsx
autonomous: false
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
  - CHAT-08
user_setup: []

must_haves:
  truths:
    - "Sidebar 渲染真实会话列表，按 today / yesterday / week / older 分组"
    - "点击会话项切换 active；旧 SSE 连接被 React effect cleanup 自然关闭"
    - "hover 会话项时右侧显示删除按钮；点击触发乐观删除 + sonner 8s 撤销 toast"
    - "点击「新建会话」生成本地占位 session_id；首次发消息后由后端幂等 create 持久化"
    - "首次进入页面若 sessions 非空则自动 setActive(sessions[0]) + handleSwitch"
    - "切换到含未完成 task 的会话时，loadHistory 后若 `truncate_after_active_task=true` 则丢弃最末 assistant message 并 reattach 该 task_id 触发 SSE 重建"
    - "Success Criteria 1-4 在 dev 环境手验通过"
  artifacts:
    - path: "frontend/src/components/sidebar/session-item.tsx"
      provides: "单条会话项（hover 显示删除按钮 + active 左侧品牌色轨）"
      contains: "stopPropagation"
    - path: "frontend/src/components/sidebar/session-group.tsx"
      provides: "分组标题 + 组内列表容器"
      contains: "SessionGroup"
    - path: "frontend/src/components/sidebar/sidebar.tsx"
      provides: "真实列表渲染（替换占位骨架）"
      contains: "groupSessions"
    - path: "frontend/src/app/page.tsx"
      provides: "handleSwitch / handleDelete / handleNew + 首次自动选中 useEffect"
      contains: "handleSwitch"
  key_links:
    - from: "frontend/src/app/page.tsx"
      to: "useSSE(currentTaskId, activeSessionId)"
      via: "双参数调用（Plan 03 Task 3 新签名）"
      pattern: "useSSE\\("
    - from: "frontend/src/app/page.tsx handleDelete"
      to: "sonner toast({ duration: 8000, action })"
      via: "撤销按钮回调 restoreSession"
      pattern: "duration: 8000"
    - from: "frontend/src/components/sidebar/session-item.tsx onClick 删除按钮"
      to: "e.stopPropagation()"
      via: "避免冒泡触发 handleSwitch"
      pattern: "stopPropagation"
---

<objective>
Phase 10 Wave 2 前端 UI 组合。基于 Plan 02（vitest）/ Plan 03（数据层）的产出，填充真实 sidebar 组件、装配 page.tsx handleSwitch/handleDelete/handleNew、实现 sonner 撤销 toast、首次进入页面自动选中最近会话，最后通过 checkpoint:human-verify 把 Success Criteria 1-4 在 dev 环境手验过。

Purpose: Plan 01/03 落地的是骨架；用户感知的 Phase 10 价值完全由本 Plan 的视觉与交互承载（侧边栏、切换、撤销）。所有 Success Criteria 的 VALIDATION 命令都标记为"手验（DESIGN 细节）"，因此本 Plan autonomous=false，末尾 human-verify 是 blocking gate。
Output: 可用的会话侧边栏 + page.tsx 组合完成 + dev 环境手验 4 条 Success Criteria 全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-session-management/10-CONTEXT.md
@.planning/phases/10-session-management/10-RESEARCH.md
@.planning/phases/10-session-management/10-VALIDATION.md
@DESIGN.md

@frontend/src/components/sidebar/sidebar.tsx
@frontend/src/app/page.tsx
@frontend/src/stores/chat-store.ts
@frontend/src/stores/session-store.ts
@frontend/src/hooks/use-sse.ts
@frontend/src/lib/time-group.ts
@frontend/src/lib/api.ts
@frontend/src/lib/types.ts

<interfaces>
<!-- Plan 03 已落地的数据层（本 Plan 只消费，不改动）-->
```typescript
// session-store.ts
useSessionStore((s) => s.sessions);                    // Session[]
useSessionStore((s) => s.activeSessionId);             // string
useSessionStore((s) => s.deletedPending);              // Session | null
useSessionStore((s) => s.loadSessions);                // () => Promise<Session[]>
useSessionStore((s) => s.setActive);                   // (id) => void
useSessionStore((s) => s.createLocal);                 // () => string
useSessionStore((s) => s.deleteOptimistic);            // (id) => Promise<void>
useSessionStore((s) => s.restoreSession);              // (Session) => Promise<void>

// chat-store.ts
useChatStore((s) => s.loadHistory);                    // (messages) => void (一次性 set)

// api.ts
loadHistory(sessionId): Promise<HistoryResponse>       // {messages, active_task, truncate_after_active_task}

// hooks/use-sse.ts
useSSE(taskId: string | null, sessionId: string): void
```

<!-- CONTEXT §D-09 切换会话行为顺序（严格遵守）-->
handleSwitch(id):
  ① 旧 SSE 由 React effect cleanup 自然关闭（不要手动 close）
  ② chat-store 清空 messages / errorMessage（`loadHistory([])` 空数组即可）
  ③ session-store.setActive(id)
  ④ await loadHistory(id) 拉历史
  ⑤ 若 `truncate_after_active_task` 则丢弃最末 assistant message；若 `active_task` 有值则 setCurrentTaskId(active_task.task_id) 触发 useSSE 重新连接 `from_id=0`

<!-- CONTEXT §D-06 active 项高亮 Linear token（直接用 CSS var 不造新色） -->
- 非 active：bg 透明，hover: bg var(--color-bg-hover)
- active: bg rgba(255,255,255,0.05)（或等价 Linear token），左侧 border-left: 2px solid var(--color-accent) / #5e6ad2
- 会话项高度 32-36px，padding horizontal 8-12px，字号 14px/font-weight 510 `var(--color-text-secondary)`

<!-- CONTEXT §D-11 删除撤销 sonner pattern -->
toast("已删除 " + (session.title || "新会话"), {
  duration: 8000,
  action: { label: "撤销", onClick: async () => { await restoreSession(session); } },
});
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: session-item.tsx + session-group.tsx（视觉基元）</name>
  <files>
    frontend/src/components/sidebar/session-item.tsx
    frontend/src/components/sidebar/session-group.tsx
  </files>
  <read_first>
    - DESIGN.md（完整：颜色 token、字重、间距规则）
    - frontend/src/components/ui/button.tsx（shadcn Button，用于删除图标按钮的基底）
    - frontend/src/components/sidebar/sidebar.tsx（当前骨架）
    - .planning/phases/10-session-management/10-CONTEXT.md §D-05, D-06, D-11, D-13
  </read_first>
  <action>
    1. 新建 `frontend/src/components/sidebar/session-item.tsx`：
       ```tsx
       "use client";
       import { Trash2 } from "lucide-react";
       import type { Session } from "@/lib/types";

       interface Props {
         session: Session;
         active: boolean;
         onSelect: (id: string) => void;
         onDelete: (id: string) => void;
       }

       export function SessionItem({ session, active, onSelect, onDelete }: Props) {
         const title = session.title || "新会话";
         return (
           <div
             role="button"
             tabIndex={0}
             onClick={() => onSelect(session.id)}
             onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") onSelect(session.id); }}
             className={[
               "group relative flex items-center justify-between gap-2",
               "h-8 px-3 rounded-md cursor-pointer",
               "text-[14px] font-[510]",
               active
                 ? "bg-[rgba(255,255,255,0.05)] text-[var(--color-text-primary)] border-l-2 border-l-[#5e6ad2]"
                 : "text-[var(--color-text-secondary)] hover:bg-[var(--color-bg-hover)]",
             ].join(" ")}
           >
             <span className="truncate">{title}</span>
             <button
               type="button"
               aria-label="删除会话"
               onClick={(e) => { e.stopPropagation(); onDelete(session.id); }}
               className="opacity-0 group-hover:opacity-100 transition-opacity p-1 rounded hover:bg-[var(--color-bg-hover-strong)]"
             >
               <Trash2 size={14} className="text-[var(--color-text-tertiary)]" />
             </button>
           </div>
         );
       }
       ```
       **关键检查点**：
       - `e.stopPropagation()` 必须在删除按钮 onClick 的 **第一行**（D-13）
       - `role="button"` + `tabIndex=0` + keydown 回车/空格触发 onSelect（a11y 基本盘）
       - 删除按钮 `aria-label="删除会话"`
       - Token 写法直接用 `var(--color-...)`，不要 hardcode 十六进制（除 `#5e6ad2` Linear 品牌色轨 CONTEXT §D-06 明确提供）
       - 如果 DESIGN.md 未定义 `--color-bg-hover` / `--color-bg-hover-strong`，改用 `rgba(255,255,255,0.03)` / `rgba(255,255,255,0.08)` 作为 fallback（并在 SUMMARY 记录决定）。
    2. 新建 `frontend/src/components/sidebar/session-group.tsx`：
       ```tsx
       "use client";
       import type { Session } from "@/lib/types";
       import { SessionItem } from "./session-item";

       interface Props {
         label: string;
         items: Session[];
         activeId: string;
         onSelect: (id: string) => void;
         onDelete: (id: string) => void;
       }

       export function SessionGroup({ label, items, activeId, onSelect, onDelete }: Props) {
         return (
           <div className="flex flex-col gap-0.5">
             <div className="px-3 py-1 text-[12px] font-[510] text-[var(--color-text-quaternary)] uppercase tracking-wide">
               {label}
             </div>
             {items.map((s) => (
               <SessionItem
                 key={s.id}
                 session={s}
                 active={s.id === activeId}
                 onSelect={onSelect}
                 onDelete={onDelete}
               />
             ))}
           </div>
         );
       }
       ```
    3. 运行 `cd frontend && npx tsc --noEmit` —— 这两个新组件自身应零 error（page.tsx 的 TS error 会在 Task 2 清除）。
  </action>
  <verify>
    <automated>grep -c "stopPropagation" frontend/src/components/sidebar/session-item.tsx | xargs -I{} test {} -ge 1 && grep -c "aria-label" frontend/src/components/sidebar/session-item.tsx | xargs -I{} test {} -ge 1 && grep -c "SessionGroup" frontend/src/components/sidebar/session-group.tsx | xargs -I{} test {} -ge 1 && echo PASS</automated>
  </verify>
  <done>
    两个组件文件创建；SessionItem 删除按钮 onClick 首行是 stopPropagation；删除按钮含 aria-label；active 状态包含 `border-l-2` + `#5e6ad2`；session-item.tsx 对应 tsc 无 error。
  </done>
</task>

<task type="auto">
  <name>Task 2: sidebar.tsx 填真实列表 + page.tsx 组合 handleSwitch/handleDelete/handleNew + 首次自动选中</name>
  <files>
    frontend/src/components/sidebar/sidebar.tsx
    frontend/src/app/page.tsx
  </files>
  <read_first>
    - frontend/src/app/page.tsx（全文；当前 `useChatStore.activeSessionId` 需改 `useSessionStore.activeSessionId`）
    - frontend/src/components/sidebar/sidebar.tsx（当前骨架）
    - frontend/src/stores/session-store.ts（Plan 03 产出）
    - frontend/src/lib/api.ts::loadHistory / listSessions
    - frontend/src/hooks/use-sse.ts（Plan 03 双参数签名）
    - .planning/phases/10-session-management/10-CONTEXT.md §D-07..D-16, §P-02, §P-04, §P-07
    - .planning/phases/10-session-management/10-RESEARCH.md §Pattern 3 / §Pattern 6 / §Anti-Patterns
  </read_first>
  <action>
    1. `frontend/src/components/sidebar/sidebar.tsx` 重写：
       ```tsx
       "use client";
       import { useEffect } from "react";
       import { Bot, Plus } from "lucide-react";
       import { Button } from "@/components/ui/button";
       import { useSessionStore } from "@/stores/session-store";
       import { groupSessions } from "@/lib/time-group";
       import { SessionGroup } from "./session-group";

       interface Props {
         onSwitch: (id: string) => void;
         onDelete: (id: string) => void;
         onNew: () => void;
       }

       export function Sidebar({ onSwitch, onDelete, onNew }: Props) {
         const sessions = useSessionStore((s) => s.sessions);
         const activeId = useSessionStore((s) => s.activeSessionId);
         const loadSessions = useSessionStore((s) => s.loadSessions);

         useEffect(() => {
           void loadSessions().catch(() => { /* 首次加载失败不阻塞 UI */ });
         }, [loadSessions]);

         const groups = groupSessions(sessions);

         return (
           <aside className="flex h-screen w-[260px] flex-col gap-4 bg-[var(--color-bg-deepest)] p-4">
             <div className="flex items-center gap-2 text-[15px] font-[510] text-[var(--color-text-quaternary)]">
               <Bot size={16} />
               <span>AI Agent</span>
             </div>

             <Button
               type="button"
               variant="ghost"
               className="w-full justify-start text-[15px] font-[510] text-[var(--color-text-secondary)] hover:bg-[var(--color-bg-hover)] hover:text-[var(--color-text-primary)]"
               onClick={onNew}
             >
               <Plus size={16} />
               新建会话
             </Button>

             <div className="flex flex-col gap-3 overflow-y-auto">
               {groups.map((g) => (
                 <SessionGroup
                   key={g.group}
                   label={g.label}
                   items={g.items}
                   activeId={activeId}
                   onSelect={onSwitch}
                   onDelete={onDelete}
                 />
               ))}
             </div>
           </aside>
         );
       }
       ```
    2. `frontend/src/app/page.tsx` 改写关键位：
       - import `useSessionStore`、`loadHistory as apiLoadHistory`、`Sidebar`、`toast`（已有）。
       - activeSessionId 改从 session-store 拿：
         ```tsx
         const activeSessionId = useSessionStore((s) => s.activeSessionId);
         const setActive = useSessionStore((s) => s.setActive);
         const createLocal = useSessionStore((s) => s.createLocal);
         const deleteOptimistic = useSessionStore((s) => s.deleteOptimistic);
         const restoreSession = useSessionStore((s) => s.restoreSession);
         const sessions = useSessionStore((s) => s.sessions);
         const loadSessions = useSessionStore((s) => s.loadSessions);
         const loadHistoryAction = useChatStore((s) => s.loadHistory);
         ```
       - 替换 `useSSE(currentTaskId);` 为 `useSSE(currentTaskId, activeSessionId);`
       - **首次自动选中（P-04）** 放在 `useEffect`：
         ```tsx
         const didInitRef = useRef(false);
         useEffect(() => {
           if (didInitRef.current) return;
           didInitRef.current = true;
           void (async () => {
             const list = await loadSessions().catch(() => []);
             if (list.length > 0) {
               await handleSwitch(list[0].id);
             }
             // 空列表 → 保持 session-store 初始的 createLocal id；AppLayout 聚焦输入框由 ChatInput 自治处理
           })();
         }, []);   // eslint-disable-line react-hooks/exhaustive-deps  —— 首次 mount 专用
         ```
       - `handleSwitch(id)` 严格按 D-09 五步（在组件内定义为 useCallback 或普通函数）：
         ```tsx
         const handleSwitch = async (id: string) => {
           // ① 旧 SSE：由 useSSE effect cleanup 自然关闭（setActive 会更新 sessionId 触发 effect 重建 + 旧 eventSource.close）
           // ② 清空 chat-store
           loadHistoryAction([]);
           // ③ 切换 active
           setActive(id);
           setCurrentTaskId(null);
           // ④ 拉历史
           try {
             const hist = await apiLoadHistory(id);
             const msgs = hist.truncate_after_active_task && hist.messages.length > 0
               ? hist.messages.slice(0, -1)
               : hist.messages;
             loadHistoryAction(msgs);
             // ⑤ reattach
             if (hist.active_task?.task_id) {
               setCurrentTaskId(hist.active_task.task_id);
               setStatus(hist.active_task.status === "interrupted" ? "interrupted" : "streaming");
             }
           } catch (err) {
             const m = err instanceof Error ? err.message : "加载历史失败";
             toast.error(m);
           }
         };
         ```
       - `handleDelete(id)`：
         ```tsx
         const handleDelete = async (id: string) => {
           const target = sessions.find((s) => s.id === id);
           if (!target) return;
           try {
             await deleteOptimistic(id);
           } catch {
             toast.error("删除失败");
             return;
           }
           // sonner 8s 撤销
           toast(`已删除 ${target.title || "新会话"}`, {
             duration: 8000,
             action: {
               label: "撤销",
               onClick: async () => { await restoreSession(target); },
             },
           });
           // 若删的是当前 active —— 落到列表下一条（sessions 是删除前快照，取除 id 外的第一项）
           if (id === activeSessionId) {
             const next = sessions.find((s) => s.id !== id);
             if (next) {
               await handleSwitch(next.id);
             } else {
               const newId = createLocal();
               loadHistoryAction([]);
               setCurrentTaskId(null);
             }
           }
         };
         ```
       - `handleNew()`：
         ```tsx
         const handleNew = () => {
           const id = createLocal();
           loadHistoryAction([]);
           setCurrentTaskId(null);
         };
         ```
       - JSX 外层包一层 `<Sidebar onSwitch={handleSwitch} onDelete={handleDelete} onNew={handleNew} />` 替代或补充现有 AppLayout 的 sidebar 渲染。**核查**：若 AppLayout 内部已经渲染了 `<Sidebar />`（无 props），需要改成 props 版本 —— 由本 Task 统一：在 page.tsx 里直接渲染 `<AppLayout sidebar={<Sidebar onSwitch={...} .../>}>` 或重构 AppLayout 接受 `sidebar` 参数。**实施推荐**：读 AppLayout 实现后选最小改动路径 —— 若 AppLayout 直接硬编码 `<Sidebar />`，改 AppLayout 接受 `sidebar?: React.ReactNode` prop 并 `{sidebar ?? <Sidebar onSwitch={noop} ... />}` 提供默认空 handler（Plan 04 实际用 page.tsx 传进来的版本）。
       - 移除所有对 `useChatStore((state) => state.activeSessionId)` 的引用（已不存在 —— Plan 03 Task 2 删除）。
       - 移除 `reset` 按钮对 `chat-store.reset` 的依赖（reset 已不生成 activeSessionId；handleNew 取代原 reset 语义 + createLocal）。
    3. 构建 + 类型检查：
       ```bash
       cd frontend && npx tsc --noEmit
       cd frontend && npm run build
       ```
       两者都必须退出码 0。
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build 2>&1 | tail -20 | grep -E "Compiled successfully|✓"</automated>
  </verify>
  <done>
    `npx tsc --noEmit` 无 error；`npm run build` 退出 0；sidebar.tsx 渲染真实会话列表；page.tsx 含 handleSwitch/handleDelete/handleNew + 首次 useEffect；`grep -n "useSSE(currentTaskId, activeSessionId)" frontend/src/app/page.tsx` 命中 1 次；`grep -n "duration: 8000" frontend/src/app/page.tsx` 命中 1 次。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Success Criteria 1-4 手验（dev 环境）</name>
  <files>（无代码变更 — 纯手验）</files>
  <action>见 <how-to-verify>：在 dev 环境逐条勾选 Success Criteria 1-4 共 20 项。</action>
  <verify>所有 20 条勾选项通过，回复 "approved"。</verify>
  <done>Success Criteria 1-4 全部手验通过；任何失败项按 resume-signal 格式回报。</done>
  <what-built>
    Phase 10 全功能：侧边栏会话列表 + 分组 + 新建 + 切换 + 删除 + 撤销 + 历史加载 + 未完成 task reattach。
  </what-built>
  <how-to-verify>
    **前置准备**：
    1. `cd docker && docker-compose up -d && cd ..`
    2. `cd backend && python -m app.main`（端口 8001）
    3. `cd frontend && npm run dev`（端口 3000）
    4. 浏览器打开 `http://localhost:3000`，打开 DevTools → Network 面板，勾选 "Fetch/XHR" 和 "EventStream"

    **Success Criteria 1: 侧边栏展示会话列表 + 新建**
    - [ ] 首次进入：若之前有 session 历史，侧边栏按 "今天/昨天/7 天内/更早" 分组显示；若无，仅显示"新建会话"按钮（首次无历史状态）
    - [ ] 发几条消息后（title 被自动写入 query 前 30 字符），会话出现在"今天"分组
    - [ ] 点击"新建会话"按钮：sidebar 立即新增一条无标题占位；输入框可继续发消息
    - [ ] 会话项 hover：背景变深；active 项左侧有 2px `#5e6ad2` 品牌色轨 + 稍深背景

    **Success Criteria 2: 切换会话 + 历史加载**
    - [ ] 点击另一条会话：输入框下方的 message list 立即清空并重新渲染该会话的历史消息（user/assistant 气泡 + tool pill）
    - [ ] 历史中的 tool pill 显示 done 状态（绿色 ✓）；reject 过的 tool 显示 rejected 状态（红色 ×）
    - [ ] 历史中 **不** 显示 HitlCard（P-01 降级为两态）
    - [ ] 首次进入页面若有历史 session，自动选中最新一条并加载其历史（P-04）

    **Success Criteria 3: SSE 连接正确关闭，不出现泄漏**
    - [ ] DevTools Network 面板 → 筛选 EventStream → 会看到一个 `/api/chat/stream/{task_id}` 连接
    - [ ] 发消息期间切换到另一个会话：旧连接状态变为 `(canceled)`，Network 列表中**不**应有两个 pending SSE 连接同时存在
    - [ ] 切回原会话（若原 task 仍 running/interrupted）：新开一个 SSE 连接，`from_id=0` 重放事件
    - [ ] HITL 场景：A 会话触发 HITL → 切到 B 会话 → 切回 A → HitlCard 仍可见且 approve/reject 按钮可用；**只有一张 HitlCard**（不重复）

    **Success Criteria 4: 删除会话 + 撤销**
    - [ ] hover 会话项右侧出现垃圾桶图标
    - [ ] 单击垃圾桶：该会话立即从列表消失；屏幕底部出现 sonner toast "已删除 [title]" + "撤销" 按钮
    - [ ] 点击撤销：会话重新出现在列表；切到该会话能看到原消息历史
    - [ ] 删除后不点撤销，等 ~10 秒：toast 自动消失；会话不再出现
    - [ ] 删除当前活跃会话：自动切到列表下一条（按 last_updated 排序）；若列表空则进入"新建会话"空态
    - [ ] 点击删除按钮时**不**触发切换（stopPropagation 生效）

    **回归验证**：
    - [ ] 单会话普通消息流不受影响（SSE token 正常逐字显示）
    - [ ] HITL approve / reject / feedback 三路径仍工作
  </how-to-verify>
  <resume-signal>
    所有 20 条勾选项全通过 → 回复 "approved"。
    有失败项 → 按以下格式列出：
      "失败：Criterion 3 第 3 条 — 切回 A 没有新开 SSE 连接，控制台报 404"
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户点击删除 | 乐观删除 —— 前端先移除 UI，再调后端；失败时回滚；deletedPending 仅存内存，刷新丢失 |
| 撤销窗口 | sonner duration: 8000 ms 内，restoreSession 可幂等恢复（Plan 01 POST 已实现幂等） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | R (Repudiation) | 删除后撤销窗口 8s | accept | 单用户 dev 环境无审计需求；8s 是产品决策（D-11）非安全缺陷 |
| T-10-08 | D (Denial of Service) | 首次 useEffect 并发 loadSessions + handleSwitch | accept | `didInitRef` 保证只触发一次；loadSessions 失败 catch 不阻塞 UI |
| T-10-09 | I (Information Disclosure) | sonner toast 显示 session.title | accept | title 是用户自己写的 query[:30]，显示给本用户本身不算泄漏 |
| T-10-01 | I | loadHistory(id) 在切换 session 时 | mitigate | Plan 01 Task 3 的 user_id 校验兜底；本 Plan 不暴露其他用户 session_id 到 UI（session-store 的 sessions 来自 listSessions，天然按 user_id 过滤） |
</threat_model>

<verification>
所有手验步骤见 Task 3 的 `<how-to-verify>`。额外静态检查：
```bash
cd frontend && npm run build   # 必须成功
grep -n "stopPropagation" frontend/src/components/sidebar/session-item.tsx    # ≥1
grep -n "duration: 8000" frontend/src/app/page.tsx                            # ≥1
grep -n "useSSE(currentTaskId, activeSessionId)" frontend/src/app/page.tsx    # =1
grep -n "truncate_after_active_task" frontend/src/app/page.tsx                # ≥1
```
</verification>

<success_criteria>
- [x] Success Criteria 1: 侧边栏展示会话列表；新建按钮工作
- [x] Success Criteria 2: 切换会话正确加载历史；历史两态（text + tool pill）
- [x] Success Criteria 3: 切换时旧 SSE 关闭；reattach 场景 HitlCard 不重复
- [x] Success Criteria 4: 删除 + 撤销 8s 窗口；删除当前会话自动切下一条
- [x] `npm run build` 退出 0
- [x] 回归：单会话消息流 / HITL 不破
</success_criteria>

<output>
完成后创建 `.planning/phases/10-session-management/10-04-SUMMARY.md`，记录：
- 最终 Sidebar / session-item / session-group / page.tsx diff 摘要
- DESIGN token 使用清单（含任何 fallback 决定）
- 手验结果（20 项勾选状态）
- 已知 limitation（与 Phase 12 的边界 —— HITL 页面刷新恢复 / SSE 断线重连均不在 Phase 10 范围）
</output>
