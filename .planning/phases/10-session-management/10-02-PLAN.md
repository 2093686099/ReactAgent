---
phase: 10-session-management
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vitest.config.ts
  - frontend/src/lib/time-group.ts
  - frontend/src/lib/__tests__/time-group.test.ts
autonomous: true
requirements:
  - SESS-01
user_setup: []

must_haves:
  truths:
    - "frontend 可通过 `npm run test -- run` 执行一次性单元测试，退出码 0"
    - "groupSessions() 把 sessions 按 last_updated 分到 today / yesterday / week / older 四桶，组内按 last_updated 倒序"
    - "空数组输入不抛异常，返回空分组数组"
    - "一条跨天边界会话（23:59 vs 次日 00:01）分配正确（不因 setHours(0,0,0,0) 时区差而错位）"
  artifacts:
    - path: "frontend/package.json"
      provides: "devDependencies 加 vitest；scripts 加 `test`"
      contains: "vitest"
    - path: "frontend/vitest.config.ts"
      provides: "最小 vitest 配置（node 环境，不接 jsdom）"
      contains: "defineConfig"
    - path: "frontend/src/lib/time-group.ts"
      provides: "groupSessions() + TimeGroup 类型 + GROUP_LABELS"
      contains: "export function groupSessions"
    - path: "frontend/src/lib/__tests__/time-group.test.ts"
      provides: "≥5 个 vitest 用例覆盖四桶 + 边界 + 排序 + 空输入"
      contains: "describe"
  key_links:
    - from: "frontend/src/lib/time-group.ts"
      to: "frontend/src/lib/types.ts::Session"
      via: "import type { Session }"
      pattern: "from \"@/lib/types\""
    - from: "frontend/package.json (scripts.test)"
      to: "vitest CLI"
      via: "\"test\": \"vitest\""
      pattern: "\"test\":"
---

<objective>
Phase 10 Wave 0 前端测试基建最小引入（P-03 决策）。只接入 vitest（不引 jsdom / testing-library / Playwright）+ 一个 `time-group.ts` 纯函数 + 对应 vitest 单测。与 Plan 01 并行执行。

Purpose: Sidebar 的分组渲染是 Phase 10 唯一对纯函数有强需求的地方；其余前端逻辑（store actions / useSSE reattach / undo toast）依赖手验 + 代码审查。本 Plan 输出最轻量的自动化采样点，避免 vitest+jsdom+testing-library 的复杂性放大 Phase 10 的风险面。
Output: `cd frontend && npm install && npm run test -- run` 退出码 0，覆盖 5+ 个 time-group 分组断言。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-session-management/10-CONTEXT.md
@.planning/phases/10-session-management/10-RESEARCH.md
@.planning/phases/10-session-management/10-VALIDATION.md

@frontend/package.json
@frontend/src/lib/types.ts

<interfaces>
<!-- Session 类型（将在 Plan 03 Task 1 正式加入 types.ts；本 Plan 测试里先用局部类型对齐契约） -->
```typescript
// 目标形状（Plan 03 会在 types.ts 里落地；本 Plan 的 time-group.ts 先按此类型签名写，
// 本 Plan 不碰 types.ts，由 Plan 03 负责新增；此处 time-group.ts 只 import Session 类型）
export interface Session {
  id: string;
  title: string;
  created_at: number;
  last_updated: number;    // 后端 time.time() 秒级 float，前端乘 1000 得毫秒
  status: string;          // "idle" | "running" | ...
  last_task_id?: string | null;
}
```

<!-- 研究文档 §Pattern 5 原型（需按本 Plan 落地，注意 import type { Session }） -->
```typescript
export type TimeGroup = "today" | "yesterday" | "week" | "older";

const GROUP_LABELS: Record<TimeGroup, string> = {
  today: "今天",
  yesterday: "昨天",
  week: "7 天内",
  older: "更早",
};

export function groupSessions(sessions: Session[], now = Date.now()):
  { group: TimeGroup; label: string; items: Session[] }[]
```

<!-- vitest 最小配置（不用 jsdom，Phase 10 纯函数单测够用） -->
```typescript
// frontend/vitest.config.ts
import { defineConfig } from "vitest/config";
import path from "node:path";

export default defineConfig({
  test: {
    environment: "node",
    include: ["src/**/__tests__/**/*.test.ts"],
  },
  resolve: {
    alias: { "@": path.resolve(__dirname, "./src") },
  },
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 引入 vitest + time-group.ts 纯函数 + 单元测试</name>
  <files>
    frontend/package.json
    frontend/vitest.config.ts
    frontend/src/lib/time-group.ts
    frontend/src/lib/__tests__/time-group.test.ts
  </files>
  <read_first>
    - frontend/package.json（当前 devDependencies 无 vitest；scripts 无 test）
    - .planning/phases/10-session-management/10-RESEARCH.md §Pattern 5（时间分组函数原型）
    - .planning/phases/10-session-management/10-CONTEXT.md §D-05（四分组时间规则）
    - .planning/phases/10-session-management/10-CONTEXT.md §P-03（vitest 最小引入）
  </read_first>
  <behavior>
    - Test 1 (`group empty array`): `groupSessions([])` 返回 `[]`
    - Test 2 (`all buckets populated`): 构造 4 条 session 分别落 today / yesterday / week / older，断言四桶各含 1 条，label 分别是 "今天" / "昨天" / "7 天内" / "更早"
    - Test 3 (`order within bucket`): today 桶含 2 条，更新较晚的排在前（倒序）
    - Test 4 (`boundary today/yesterday`): `now=2026-04-20 10:00:00` 时；两条 session `last_updated` 分别是 `2026-04-20 00:00:00`（今天起点）和 `2026-04-19 23:59:59`（昨天末尾），前者进 today 后者进 yesterday
    - Test 5 (`boundary week/older`): 7 天前 00:00:00 进 week；7 天前前一秒进 older
    - Test 6 (`empty bucket excluded`): 仅 older 有数据 → 返回数组长度 1（不返回 today/yesterday/week 的空桶）或保留所有桶但 items=[]（实现二选一，测试断言与实现对齐）。推荐实现：**过滤空桶**（UI 渲染直接 map，无需再判空）
    - Test 7 (`last_updated is seconds not ms`): `last_updated = Date.now() / 1000`（秒级 float）被正确乘 1000 → 落 today 桶
  </behavior>
  <action>
    1. `frontend/package.json`：
       - devDependencies 添加 `"vitest": "^2.1.9"`（Next 15 + node 20 兼容最新稳定版）。
       - scripts 添加 `"test": "vitest"`。
       - **注意**：不加 jsdom / @vitest/ui / @testing-library —— P-03 最小引入。
    2. `frontend/vitest.config.ts`：按 `<interfaces>` 里的模板原样写入（`environment: "node"` / `include: ["src/**/__tests__/**/*.test.ts"]` / 别名 `@` → `./src`）。
    3. `frontend/src/lib/time-group.ts`：
       ```typescript
       import type { Session } from "@/lib/types";

       export type TimeGroup = "today" | "yesterday" | "week" | "older";

       export const GROUP_LABELS: Record<TimeGroup, string> = {
         today: "今天",
         yesterday: "昨天",
         week: "7 天内",
         older: "更早",
       };

       export interface GroupResult {
         group: TimeGroup;
         label: string;
         items: Session[];
       }

       function startOfToday(now: number): number {
         const d = new Date(now);
         d.setHours(0, 0, 0, 0);
         return d.getTime();
       }

       export function groupSessions(sessions: Session[], now: number = Date.now()): GroupResult[] {
         const todayStart = startOfToday(now);
         const yesterdayStart = todayStart - 24 * 3600 * 1000;
         const weekStart = todayStart - 7 * 24 * 3600 * 1000;

         const buckets: Record<TimeGroup, Session[]> = {
           today: [],
           yesterday: [],
           week: [],
           older: [],
         };

         for (const s of sessions) {
           const tsMs = s.last_updated * 1000;   // 后端秒级 float → 毫秒
           if (tsMs >= todayStart) buckets.today.push(s);
           else if (tsMs >= yesterdayStart) buckets.yesterday.push(s);
           else if (tsMs >= weekStart) buckets.week.push(s);
           else buckets.older.push(s);
         }

         const order: TimeGroup[] = ["today", "yesterday", "week", "older"];
         return order
           .map((g) => ({
             group: g,
             label: GROUP_LABELS[g],
             items: buckets[g].sort((a, b) => b.last_updated - a.last_updated),
           }))
           .filter((r) => r.items.length > 0);
       }
       ```
       - 注意：当前 `@/lib/types` 没有 `Session` 接口；这是 **预期** —— Plan 03 Task 1 会新增。TypeScript 在 Plan 02 执行时会报错；**用 `// @ts-expect-error Session 类型在 Plan 03 添加` 暂时标注**，或在本 Plan 的 time-group.ts 顶部加局部 `type Session = { id: string; title: string; created_at: number; last_updated: number; status: string; last_task_id?: string | null };` 并在 Plan 03 合并时移除局部 type 改为 import。**实施推荐**：用局部 type（更干净，避免 ts-expect-error 漏网；Plan 03 Task 1 落地 types.ts 后回来删局部 type 改 import）。
    4. `frontend/src/lib/__tests__/time-group.test.ts`：
       ```typescript
       import { describe, it, expect } from "vitest";
       import { groupSessions } from "@/lib/time-group";

       function mkSession(id: string, lastUpdatedSec: number) {
         return {
           id, title: id, created_at: 0, last_updated: lastUpdatedSec, status: "idle",
           last_task_id: null,
         };
       }

       describe("groupSessions", () => {
         const NOW = new Date("2026-04-20T10:00:00+08:00").getTime();

         it("returns empty array for empty input", () => { ... });
         it("populates all four buckets with one item each", () => { ... });
         it("sorts items within bucket by last_updated desc", () => { ... });
         it("today/yesterday boundary at midnight", () => { ... });
         it("week/older boundary at 7 days", () => { ... });
         it("excludes empty buckets from result", () => { ... });
         it("treats last_updated as seconds (multiplies by 1000)", () => { ... });
       });
       ```
       - 实现每个 `it` 的 arrange / act / assert，使用 `NOW` 作为 `groupSessions(sessions, NOW)` 第二参数（关键：测试不依赖"真实现在时间"，否则时间推进会导致测试飘红）。
       - `last_updated` 字段用 `NOW / 1000` / `(NOW - 24 * 3600 * 1000) / 1000` 等构造（保持秒级精度）。
    5. 本地验证：
       ```bash
       cd frontend && npm install && npm run test -- run
       ```
       应当看到 7 个 passing test。
  </action>
  <verify>
    <automated>cd frontend && npm install --silent && npm run test -- run</automated>
  </verify>
  <done>
    vitest 成功执行；`time-group.test.ts` 至少 6 个用例全绿（`excludes empty buckets` 实现与测试对齐）；`grep -n "groupSessions" frontend/src/lib/time-group.ts` 命中导出声明；`frontend/vitest.config.ts` 存在且 `environment: "node"`。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

本 Plan 仅引入开发依赖（vitest）与一个纯函数 + 单测；不涉及新的运行时信任边界。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | T (Tampering) | `devDependencies` 新增 vitest 包 | accept | vitest 是主流开源库（Vite 团队维护）；由 `package-lock.json` 锁版本；不进入生产 bundle |
</threat_model>

<verification>
```bash
cd frontend && npm run test -- run
# expect: Test Files  1 passed (1), Tests  7 passed
```

静态检查：
```bash
grep -n "\"test\":" frontend/package.json
grep -n "groupSessions" frontend/src/lib/time-group.ts
```
</verification>

<success_criteria>
- [x] `cd frontend && npm install && npm run test -- run` 退出码 0
- [x] 7 个用例全绿（含空输入 / 四桶 / 桶内排序 / today-yesterday 边界 / week-older 边界 / 空桶过滤 / 秒转毫秒）
- [x] vitest 配置 `environment: "node"`（不用 jsdom）
- [x] package.json 只新增 `vitest` 一个依赖（不引 testing-library / jsdom / @vitest/ui）
</success_criteria>

<output>
完成后创建 `.planning/phases/10-session-management/10-02-SUMMARY.md`，记录：
- package.json 变更 diff
- vitest 首次跑的输出尾部（"Test Files... Tests... passed"）
- time-group.ts 最终导出清单（groupSessions / GROUP_LABELS / TimeGroup / GroupResult）
- Plan 03 接手建议（types.ts 新增 Session 后把 time-group.ts 里的局部 type 改为 import）
</output>
